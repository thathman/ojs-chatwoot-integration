# SETTINGS CONSOLE REAL-BROWSER FAILURE — 2026-09-04

Owner-observed browser state: FAIL.

The effective OJS Chatwoot Integration settings page still renders the legacy single-scroll UI. This is real-browser evidence that the Settings Console is not accepted, regardless of merged source slices or HTTP 200 health.

Visible legacy indicators include untranslated locale keys, raw numeric Chatwoot IDs, negative Hide-for audience controls, optional Privacy Mode/Blind Review checkbox, legacy retry controls, raw per-event JSON, raw Widget Settings JSON, technical loading/CSP/route controls in the main workflow, and old import/export/global-profile presentation.

Treat this as the baseline screenshot to beat during item K. The final acceptance artifact must show the effective OJS page rendering the new console with B–J complete and no stale theme override shadowing it.
