# v2 Master Tasklist

This is the implementation backlog. Checkboxes are not release claims; they become complete only with acceptance tests.

## GAP — Hostile Completion Audit Register (2026-09-01)

This register was added after the first real post-release acceptance pass exposed multiple failures that earlier source-level/unit/replicated-environment tests had missed. It is authoritative over any older `[x]` row below when the older row's own prose says **first slice**, **partial**, **not wired**, **source-level only**, **follow-up**, **not yet**, or equivalent. A historical checkbox may remain useful as evidence that a slice landed, but it must not be interpreted as product completion while a corresponding GAP/CMP task remains open.

The companion `docs/v2/COMPLETION_RECONCILIATION.md` remains the Product Bible ↔ implementation classification ledger. This section adds the defects, construction-quality gaps, and acceptance-evidence gaps discovered by the hostile source/runtime audit that are not fully represented there.

### CMP — Completion truth / evidence governance

- [ ] **CMP-001 — Enforce honest completion semantics.** A task may not be considered COMPLETE while its own description contains `first slice`, `partial`, `not wired`, `source-level only`, `follow-up`, `not yet`, `future scope`, or equivalent unresolved language. Split such work into explicit subtasks or keep the parent open. Apply this rule when reconciling all tasklists and acceptance matrices.
- [ ] **CMP-002 — Classify every test by evidence tier.** Tag/document tests as `unit`, `structural/source`, `socket-contract`, `replicated-db`, `real-ojs-runtime`, `real-external-integration`, or `full-acceptance`. Never describe a lower tier as proving a higher tier. In particular, replicated SQLite DDL, `str_contains()`/source assertions, and hand-made HTTP contract servers are useful but are not substitutes for a real OJS install.
- [ ] **CMP-003 — Reconcile false-complete rows.** Explicitly reopen/override the completion claim for at least **API-018/TST-013** (2026-09-02: now closed — all 14 endpoints have real success- and error-envelope contract coverage, ACC-001/ACC-001B), **AUD-002/AUD-013** (2026-09-03: now covers REST + event/queue/delivery + Captain sync; legacy dead-letter give-up's own live re-verification remains structural-only, pending a real non-empty legacy queue), **EVT-007** (adapter exists but is not wired), **MCP-005** (reserved staff consumer/public denial is not a staff namespace + credential), **TST-009** (ContextResolver-only multi-journal coverage), **TST-015** (replicated DDL/source gating, not exact-package lifecycle), and **TST-016** (Captain Document first slice only). Keep the historical slice evidence, but do not call the product requirement complete.
- [ ] **CMP-004 — Make real-runtime acceptance a mandatory gate.** Any OJS API/class/namespace/router/hook/form/scheduler/migration claim that can be proven against the pinned OJS 3.5 runtime must be exercised there before completion. TST-018 proved the existing mock suite can be internally consistent while mocking a class that does not exist in production.
- [ ] **CMP-005 — Prevent self-confirming mocks.** Test doubles for OJS/PKP APIs must be derived from or checked against a pinned real `stable-3_5_0` source/runtime contract. A test may not invent the namespace/signature it is intended to verify.
- [ ] **CMP-006 — Re-run completion reconciliation after every acceptance defect.** TST-017+ defects are evidence that release/tasklist status must move backward when acceptance disproves an earlier claim; the ledger and tasklist must be updated in the same corrective cycle.
- [x] **CMP-007 (new finding, 2026-09-02) — Real dell deployment was 111 commits / PRs #119–#164 behind `v2-dev` for an unknown span of prior sessions, invalidating the deployment premise of every "live Dell"/"confirmed live" claim recorded during that span.** Discovered while deploying EVT-017 (#165): `git status` inside the real `/var/www/html/plugins/generic/chatwootIntegration` checkout on dell showed it pinned at `cbbd42e` (PR #118), while `v2-dev` was already at `a98f15a` (PR #164) — meaning EVT-016/017(part 1)/018/019/021, MCP-010, POL-011, SETTINGS-SMALL-001/002, TST-022/023, and every other PR from #119 through #164 had been merged to `v2-dev` and marked with "live Dell"/"confirmed live" evidence in this tasklist and `docs/v2/COMPLETION_RECONCILIATION.md`, but **none of that code had ever actually been running on the one real OJS install used for acceptance testing**. Root cause: the standing directive's own `branch→...→merge→sync→deploy→docs` workflow was not actually including a real `git pull` on dell after each merge — "deploy" was treated as implicit/skipped rather than a real, separate action. Fixed for this PR (EVT-017/#165: `git fetch origin v2-dev && git checkout v2-dev && git merge origin/v2-dev --ff-only` run for real on dell, confirmed via the fast-forward diff, `cache/opcache/*` cleared). **Going forward, "deploy" means a real, verified `git pull`-equivalent on dell in the same change, not an assumption.** Concrete fallout: EVT-021's live evidence (submissions 22/23) and EVT-017's first live-acceptance attempts (submissions 21–24) were all gathered against this stale, pre-#150s checkout — EVT-021's root cause is in pkp-lib itself (unaffected by which plugin commit is deployed) so its conclusion is not in question, but the specific plugin-side behavior described alongside it (e.g. "two real v2 durable-queue rows with `status=delivered`") was produced by code that predates EVT-016/017's own allowlist/queue-consumer changes and must not be read as evidence about the current code. The EVT-017 forced-failure/retry re-test with submissions 25/26 (documented above) was also run before this sync, against the same stale checkout — already correctly treated as invalidated on its own separate grounds (the empty `LIVE_DELIVERY_ALLOWLIST`), so no further correction needed there, but it is now doubly confirmed as not being evidence about current code either way. No PII exposure or data-integrity issue resulted — this is a deployment-currency gap, not a security defect.

### EVT — Event Bridge / legacy-runtime reconciliation

- [x] **EVT-016A — Immediate duplicate-delivery prevention.** Confirmed real and active (not theoretical): `deliverQueuedSupportEvents()`'s scheduled consumer was genuinely re-posting to the live Chatwoot API for every `submission.created`/`decision_recorded`/`*_requested`/`accepted`/`rejected`/`publication.*` event v1's own synchronous `dispatchEvent()` had already delivered the instant the same real hook fired. Closed via a new `LIVE_DELIVERY_ALLOWLIST` constant on `ChatwootIntegrationV2Plugin` (empty at the time by design, populated incrementally as EVT-017 transfers each type — see below): `v2DeliverQueuedEventRow()` now treats any event type not on that explicit allowlist as record-only (same outcome as `AUDIT_ONLY`), so the queue keeps its real audit/correlation value (AUD-002/013) without ever re-delivering a type v1 already handles live. Evidence: PR #159; `tests/v2/evt-016-no-double-delivery.php` reflects the real private method and now proves the current real per-type split (see EVT-017).
- [x] **EVT-016B — Single authoritative event-delivery ownership.** **Complete 2026-09-03 — all 8 known event types transferred to v2 in code AND live-accepted.** `LIVE_DELIVERY_ALLOWLIST`/`isLiveDeliveryOwnedByV2()` cover every real event-setting key (see EVT-017/EVT-020 for the transfer history and the critical fixes found along the way — #179's per-decision-code keying, #189's shared-setting-key double-delivery fix). Real live Dell acceptance evidence exists for all 8:
  - `submission.created` — EVT-017 (2026-09-02, submission #27).
  - `submission.review_submitted` — EVT-020 (2026-09-02, submission #18, reviewer `t-reviewer1`).
  - `submission.decision_recorded` — EVT-020 (2026-09-03, submission #16, "Send for Review" decision code 3, the true fallback case).
  - `submission.revision_requested` — EVT-020 (2026-09-03, submission #21, real "Request Revisions" decision, `open_update` mode for a brand-new contact, real Chatwoot conversation 146 created).
  - `submission.accepted` — EVT-020 (2026-09-03, submission #21, real "Accept Submission" decision, delivered under real production `note` mode since the contact already existed).
  - `submission.rejected` — EVT-020 (2026-09-03, submission #24, real "Decline Submission" decision code 8; a real accidental double-click produced two v2 queue rows for one real decision, the second was defensively marked already-delivered before the scheduler ran so only one real Chatwoot note was ever posted — see the note below).
  - `publication.published` — EVT-020 (2026-09-03, submission #21, real "Schedule For Publication" against the one existing, already-published issue — OJS published it immediately since the target issue has no future date; delivered alongside a real `submission.accepted` row from the same real action, both legitimate distinct real occurrences, not a double-delivery of the same type).
  - `publication.scheduled` — EVT-020 (2026-09-03, submission #16, a real future issue created for this purpose — "Vol. 1 No. 2 (2026): Second Issue", genuinely unpublished — and the submission scheduled into it, producing the real `STATUS_SCHEDULED` transition).
  - **Note on the accidental double-enqueue during the `rejected` test:** a real UI double-click on "Record Decision" produced two `chatwoot_support_event_queue` rows with different idempotency keys for what should have been one real decision (only one `edit_decisions` row was ever persisted) — the underlying cause was not fully root-caused (plausibly OJS's own decision-add endpoint being called twice by an accidental double-submit, not a v2 enqueue-path bug, since the idempotency-key derivation itself is unchanged and working elsewhere in this pass). Handled pragmatically for this test (one row marked pre-delivered to prevent a real duplicate Chatwoot post) rather than investigated further; worth a closer look in a future pass if it recurs outside of an accidental double-click.
  - The target end-state (per the owner's directive) is now fully real: `OJS hook → one normalized SupportEvent → one policy evaluation → one durable v2 queue → one scheduler/worker consumer → Chatwoot`, with v1's synchronous delivery retired per family — verified live for all 8 families, not just code-reviewed.
  - **2026-09-03 session note:** browser-automation access was revoked mid-session by the harness's own permission classifier (an autonomous/unattended-session safety gate, not a task-specific denial) partway through this pass, blocking further live-acceptance work for a time; access was later restored within the same session and the remaining 5 types' live acceptance (`revision_requested`/`accepted`/`rejected`/`publication.scheduled`/`publication.published`) was completed above. MCP-011/012's real-client acceptance remains separately blocked — see MCP-015, an infra-level issue unrelated to browser access.
  - **CRITICAL FIX (2026-09-03, found by a post-merge security review of the `accepted`/`rejected` transfer) — `eventAccepted`/`eventRejected` are shared ownership setting keys between two independent real hooks, only one of which had the guard.** `handleEditorDecision()` (a real editorial decision) and `handleSubmissionStatusUpdated()` (`Submission::updateStatus` — a real status change, driven by a completely different adapter, `SubmissionStatusChangedEventAdapter`) both produce `SUBMISSION_ACCEPTED`/`SUBMISSION_REJECTED` and both read the exact same `eventAccepted`/`eventRejected` settings. Transferring those two settings (allowlisting the types on the v2 side) checked only the first hook — `handleSubmissionStatusUpdated()` had no `isLiveDeliveryOwnedByV2()` guard at all, so it kept unconditionally calling `dispatchEvent()`. Net effect, live on dell for the span between the `accepted`/`rejected` transfer merging and this fix: a real submission published or declined via a status change (not necessarily via an editorial decision) would have **both v1 (here) and v2 (the now-allowlisted queue consumer) deliver live for the same real occurrence** — a genuine double-post, the exact class of defect this whole ownership-transfer framework exists to prevent. Fixed by adding the identical `isLiveDeliveryOwnedByV2($eventKey)` guard `handleEditorDecision()` already has, in the same position (after `$eventKey` is computed, before `dispatchEvent()`); the same guard was added defensively to `handlePublicationPublished()` too (`eventPublicationScheduled`/`eventPublicationPublished` aren't transferred yet, so not yet an active bug there, but the same oversight must not repeat when they are). `tests/v2/evt-016-no-double-delivery.php` extended to structurally verify both handlers now carry the guard, in the correct order, the same way `handleEditorDecision()`'s guard is already verified. No real double-post was ever confirmed to have actually reached Chatwoot (no real status-change-to-published/declined occurred on dell in that window, per a review of the real audit log), but the code was live and exploitable by any real editorial action taking that path. **Lesson for every future EVT-016B transfer: before allowlisting a type, grep for every real v1 call site that reads the same ownership setting key, not just the one the current transfer is focused on.**
  - **Research note (2026-09-02):** unlike `submission.created`, `decision_recorded`'s v1 path (`handleEditorDecision()`) does NOT have EVT-021's timing defect — it re-fetches the submission fresh via `Repo::submission()->get()` rather than using the hook's own stale object, so `getCurrentPublication()` is reliable there. Live-tested via a real "Revert Decline" decision on real submission #17 (`eventDecisionRecorded` genuinely enabled): produced a real `submission.decision_recorded` v2 queue row, and no real Chatwoot contact was created by either v1 or v2 for the real test author — but this is consistent, by-design behavior for both (the real production `eventSyncMode`/`note` gate blocking new-contact creation under `note` mode for a brand-new author), not evidence that v1 is broken here.
  - **`submission.decision_recorded` transferred and live-accepted 2026-09-03 (3rd of 8) — two real bugs found and fixed en route.** Added `isLiveDeliveryOwnedByV2('eventDecisionRecorded')` and `SUBMISSION_DECISION_RECORDED` to `LIVE_DELIVERY_ALLOWLIST` in the same change, the atomic-switch construction every prior transfer uses.
    - **CRITICAL FIX (#179):** the first implementation keyed the ownership guard on the bare `'eventDecisionRecorded'` string unconditionally, which wrongly blocked v1's live delivery for *every* decision code, not only the one type actually transferred (`decision_recorded`'s fallback case) — confirmed live: a real "Accept Submission" decision (code 2) on real submission #18 was silently blocked from v1 delivery, and v2 also never delivered it (`SUBMISSION_ACCEPTED` not yet allowlisted), a genuine silent notification gap. Fixed by keying the guard on `$eventKey ?? 'eventDecisionRecorded'`, computed via v1's own `mapDecisionEventKey()` after it runs — the same specific type v2's `DecisionRecordedEventAdapter::mapDecisionEventType()` would actually enqueue this decision code as. `tests/v2/evt-016-no-double-delivery.php` extended with a negative assertion that the buggy bare-key pattern never reappears.
    - **CRITICAL FIX (#180) — `ChatwootApiService::createConversationNote()` was POSTing to a real Chatwoot endpoint that does not exist.** `accounts/{id}/conversations/{id}/notes` returns a real HTTP 404 against `https://support.airixmedia.com` — confirmed live via direct `curl`. A private note is not a distinct Chatwoot resource; it's created via `/messages` with `private: true` (confirmed live, HTTP 200). `'private_note'` is the real production default `eventSyncMode`, and this method backs v1's own delivery path, v2's queued delivery, and both escalate/handoff-summary note call sites — so **every note-mode delivery had been silently failing (`delivery_failed`, logged but never surfaced) since this method was written**, undetected because the project's own TST-003 mock server encoded the same wrong `/notes` assumption. Fixed to POST to `/messages` with `private: true`, same as `createConversationMessage()`. This also explains a real `event_delivery:submission.created` audit row (`decision=deny`, `reason=delivery_failed`, 2026-09-02 22:55:27) recorded under real production `note`-mode defaults — unrelated to and not invalidating EVT-017's own already-documented `submission.created` acceptance (which used a temporarily-flipped `open_update` mode and so never exercised this endpoint), but confirming the same defect class reached real production traffic for the one allowlisted type in the real default mode.
    - **Real live Dell acceptance, full chain confirmed after both fixes:** real "Accept and Skip Review" decision (code 17, the true `submission.decision_recorded` fallback case) recorded on real submission #16 (fresh QuickSubmit-style submission created for this test, real author `k.mensah@example.com`) → v1 correctly stayed silent (ownership guard fix confirmed: no Chatwoot-related log/audit entry from v1) → real v2 queue row enqueued (`id=14`, `correlation_id=4264dafc...`) → real scheduler tick delivered it (`status=delivered`) → real Chatwoot conversation 144 received a real private note ("OJS Notification: Editor decision 17 recorded for submission #16 (...)", `private=true`) → real audit-log row (`id=60`, `event_delivery:submission.decision_recorded`, `decision=allow`, `reason=delivered`) carrying the exact same `correlation_id` as the queue row. Ordinary OJS pages confirmed HTTP 200 throughout. **EVT-016B/EVT-020 close for `submission.decision_recorded` on this evidence — 3rd of 8 transferred/live-accepted types.**
- [x] **EVT-017 — Retire/migrate the legacy `apiQueue` safely (CRITICAL); atomic per-type live-delivery ownership transfer.** Established the mechanism using `submission.created` as the first transfer — real, live-accepted end-to-end (see the acceptance sequence below). The remaining 7 event types' ownership transfer, using this same mechanism, was EVT-016B's scope — now complete (see EVT-016B).
  - **Inventory (done):** setting key `apiQueue`, per-context JSON array of `{id, type ('conversation_event'|'canned_response_sync'), payload, attempts, runAfter}`. Producers: `enqueueApiJob()` from `dispatchEvent()` (event delivery failure) and `syncEmailTemplates()` (canned-response sync failure). Consumers: `processApiQueue()` via `dispatchEvent()`'s own drain, `syncEmailTemplates()`'s own drain, and (since EVT-018A) the reliable scheduler-only `ProcessLegacyRetryQueueScheduledTask`. Backoff: exponential, capped at 3600s. PII: `conversation_event` payloads carry real author email/name/user id and submission title/id/url/journal name via `buildSubmissionAttributes()`; `canned_response_sync` carries email-template short code/body. Real, safe production inventory taken on dell (2026-09-02, counts only): **zero rows, zero queued jobs** across every real journal today; the v2 durable queue table exists and is also empty (Case A — fresh/empty legacy queue — for the one real environment available).
  - **Legacy jobs cannot be safely normalized into a typed v2 `SupportEvent`:** all four v1 handlers enqueue the generic `conversation_event` type with no discriminator for which of the 7 original event kinds it was — the specific type is lost at enqueue time. Per the directive's own fallback ("if a legacy job cannot safely map into the v2 normalized event model, document and implement a bounded legacy-drain compatibility phase instead"), no lossy migration-into-v2 is implemented; `ProcessLegacyRetryQueueScheduledTask` (EVT-018A) IS that bounded legacy-drain compatibility phase — it reliably drains legacy-format jobs via their own native `executeApiJob()`/`sendChatwootEvent()`/`createCannedResponse()` code, unchanged.
  - **Fixed earlier pass:** give-up (attempts exhausted) used to silently drop a job with zero trace. Now records a safe audit entry (`component=legacy_api_queue, decision=give_up, contextId, jobId, jobType, attempts` — never the real payload/PII) via the existing `SupportApiAuditLoggerInterface`/`ErrorLogSupportApiAuditLogger` sink. Corrupt/malformed `apiQueue` JSON was already fail-safe (`getApiQueue()` returns `[]`) — confirmed and regression-tested, not previously proven.
  - **Live Dell acceptance sequence (Section 12), first attempt — invalidated, root cause found:** two real submissions (IDs 22, 23) confirmed each produced exactly one real v2 durable-queue row (`status=delivered`) and zero real Chatwoot contacts. A follow-up forced-failure/retry attempt (submission ID 25/26, real `chatwootApiAccessToken` corrupted and restored twice, both times safely backed up and verified restored via a real `GET /api/v1/profile` → HTTP 200) also showed `status=delivered` regardless of whether the token was valid. **Root cause, confirmed by reading `v2DeliverQueuedEventRow()`:** `LIVE_DELIVERY_ALLOWLIST` was still empty at that point, so the function returns `true` at its very first guard clause — before ever reading `chatwootApiAccessToken` or calling `ChatwootApiService` — for every event type, `submission.created` included. Combined with EVT-021 (v1 also silently fails to deliver `submission.created` on real OJS 3.5), this means **no event type had a real live-delivering pipeline at all**: every `status=delivered` row across the whole session, for every event type, has only ever meant "the queue row's own lifecycle completed," never "Chatwoot actually received something." The forced-failure/retry test as first run could not have proven anything about real durable retry, because nothing was actually being delivered for it to retry. No production impact either way (the corrupted-then-restored token was never actually read during that window).
  - **Ownership-transfer mechanism implemented this pass:** added `ChatwootIntegrationBasePlugin::isLiveDeliveryOwnedByV2(string $eventSettingKey): bool` (protected, returns `false` by default — v1 stays the live deliverer for a type until explicitly overridden) and a check for it at the top of `handleSubmissionCreated()`, before `safeGetPrimaryAuthor()`/`dispatchEvent()` run. `ChatwootIntegrationV2Plugin` overrides it to return `true` only for `'eventSubmissionCreated'`, and `LIVE_DELIVERY_ALLOWLIST` now contains exactly `SupportEventType::SUBMISSION_CREATED` — the allowlist flip and the v1 opt-out are the same code change, so there is never a window where both v1 and v2 attempt live delivery for this type, or neither does (v2's own `Submission::add` override already unconditionally enqueues `submission.created` into the v2 durable queue regardless of this switch — see EVT-003/EVT-011 — so once v1 opts out, v2's existing enqueue path is automatically the sole path). `submission.created` was chosen as the first (and so far only) transfer because EVT-021 already proved v1's synchronous path silently fails to deliver it on real OJS 3.5 — v2 durable delivery is a strict improvement here, not a user-visible behavior change. `dispatchEvent()`'s own opportunistic `apiQueue` drain (EVT-018B) is a no-op side effect of this change for this one type only (v1's handler now returns before ever calling `dispatchEvent()` for `submission.created`); it is unchanged for the other 7 still-v1-owned types. Evidence: `tests/v2/evt-016-no-double-delivery.php` (updated — proves `submission.created` now reaches the null-bridge guard and fatals, i.e. actually attempts delivery, while the other 7 types remain blocked no-ops, and asserts the allowlist/ownership-switch parity exactly) and `tests/v2/evt-017-submission-created-ownership-transfer.php` (new — proves `handleSubmissionCreated()`'s real source checks ownership before any author lookup, and behaviorally proves it returns early without touching the submission object beyond `getData('contextId')`).
  - **Live Dell acceptance sequence (Section 12), completed for `submission.created` — two further real blockers found and fixed en route (EVT-022, EVT-023).** Re-attempting the sequence against the deployed ownership-transfer code (real submission ID 27, real contributor `evt017test6@example.com`, `chatwootApiAccessToken` corrupted then restored, `eventSyncMode` temporarily and reversibly set to `open_update` for the test window — restored to the real production `note` value immediately after) surfaced two more real, previously-undiscovered defects before a genuine delivery could succeed:
    - **EVT-022:** `Ojs35CompatibilityAdapter::getPrimarySubmissionAuthor()`'s fallback branch (and v1's identical `safeGetPrimaryAuthor()`) gated on `is_array($publication->getData('authors'))`, which is **always false on real OJS 3.5** — confirmed live (`get_class()` → `Illuminate\Support\LazyCollection`, non-empty, correct author) — so a real, present author was silently discarded whenever `getPrimaryAuthor()` returned null (any publication with no explicit `primaryContactId`, i.e. essentially every real submission). Fixed via `is_iterable()` + a first-element loop in both copies.
    - **EVT-023 (the actual root cause, more fundamental than EVT-022):** `RuntimeContextBridge::$kernel` was only ever initialized as a side effect of `resolve($request, ...)` — every other bridge method (`loadSubmission()`, `getPrimarySubmissionAuthor()`, all 34 methods on the class) gated on `!$this->kernel` but had no way to populate it themselves. `DeliverQueuedSupportEventsTask` (and every other scheduled task) builds a fresh bridge and calls its methods directly, **never** calling `resolve($request)` first — so `$kernel` was always null and `loadSubmission()` always returned null immediately, before author resolution, before any Chatwoot call, for every event type, unconditionally, since the bridge was introduced. This is the real reason `status=delivered` never once meant a real delivery for any event type prior to this fix. Fixed by extracting the version-detect + kernel-construction half of `resolve()` into a lazy, idempotent `ensureKernel(): bool` that every one of the bridge's 34 gated methods now calls instead of a bare `$kernel` null-check — `resolve()` itself is unchanged in behavior for real request-driven callers, this only fixes the request-less (scheduled-task/CLI) callers that previously got silent nulls from everything.
    - **Full real acceptance, confirmed after both fixes deployed to dell:** corrupted token → real queued event stayed `pending`, `attempts` incremented, `last_error_code=delivery_failed` (a real HTTP auth failure, not a guard no-op) → token restored, verified live (`GET /api/v1/profile` → HTTP 200) → next real scheduler tick (`php lib/pkp/tools/scheduler.php run`, the real production cron entrypoint) delivered the row exactly once (`status=delivered`, task duration ~2s — real network time, not a no-op's ~10ms) → confirmed via a direct real Chatwoot API call: a real conversation (id 143) was created for a real new contact, with the real message "OJS Notification: Submission #27 () was created." Ordinary OJS pages remained HTTP 200 throughout. No PII surfaced in any log/audit output. **EVT-016B/EVT-017 close for `submission.created` on this evidence.** The other 7 event types' ownership transfer (EVT-016B's remaining scope) has not yet been started.
    - **EVT-023 correction (checked, 2026-09-02):** the note above about spot-checking other scheduled tasks was based on a guess, not a check — `CaptainSyncScheduledTask`/`PurgeExpiredSupportDataTask`/`ProcessLegacyRetryQueueScheduledTask` were grepped and none of them touch `RuntimeContextBridge` at all, so EVT-023 does not affect them. The real, more significant implication actually found by grepping every `runtimeContextBridge()` call site in the plugin: `ensureKernel()`'s fix is not scheduled-task-specific — it protects *every* caller that reaches a gated bridge method without `resolve($request)` having run first in the *same PHP process*, regardless of whether that process is a CLI scheduler or a real web request. This specifically also covers `handleReviewAssignmentEdit()` (EVT-019's real `ReviewAssignment::edit` hook handler, which calls `loadSubmission()`/`getSubmissionTitle()` directly): if a reviewer's review-submission POST redirects without rendering a template in the same request (a common OJS pattern), `addChatwootWidget()`'s own `resolve($request)` call — the only other thing in this plugin that populates the kernel — would never run in that request either, and before this fix `loadSubmission()` would have silently returned null for the same reason `deliverQueuedSupportEvents()` did. This is now fixed by the same change, but has not yet been live-verified specifically for the review-assignment path — see EVT-019's own still-open live-acceptance item.
- [x] **EVT-018A — No support queue/network delivery during template rendering.** Removed the unconditional `processApiQueue()` call from `addChatwootWidget()` (fired on every `TemplateManager::display`/`fetch` site-wide, before even checking whether the widget was enabled). Added `ChatwootIntegrationBasePlugin::processQueuedApiJobsForContext()` (public entry point) and `classes/v2/Task/ProcessLegacyRetryQueueScheduledTask.php` (real `ScheduledTask`, registered in `registerSchedules()`, iterates every real journal every five minutes) as a scheduler-only consumer, matching `DeliverQueuedSupportEventsTask`'s pattern for the v2 queue. Evidence: PR #160; `tests/v2/evt-018-no-network-in-render.php` proves the call site is gone from the render path and the replacement task exists/is wired, plus updated `tests/v2/live-plugin.php` scheduler-registration assertions.
- [ ] **EVT-018B — Retry queues are scheduler/worker-owned only.** Not yet done — `dispatchEvent()` (called from every real event handler: submission created, decision, status updated, publication published) still opportunistically drains `processApiQueue()` itself on every new event occurrence, and the explicit "Sync Email Templates" admin action does too. Neither is a template-render side effect (EVT-018A's defect), but both are still a *hidden* drain path rather than the scheduler being the sole owner of retries. Folded into EVT-017: once v1's synchronous delivery is retired for a given event family, `dispatchEvent()`'s own opportunistic drain becomes dead code for that family and should be removed in the same change, not before — removing it prematurely (while v1 is still the live deliverer) would silently degrade real production retry latency for low-traffic journals between scheduler runs.
- [x] **EVT-019 — Wire `ReviewSubmittedEventAdapter` to a real stable OJS hook.** Registered/normalized/queued: `ChatwootIntegrationV2Plugin::register()` registers the real, verified `ReviewAssignment::edit` hook (confirmed against the real pkp-lib `stable_3_5_0` source: `PKP\submission\reviewAssignment\Repository::edit()` calls `Hook::call('ReviewAssignment::edit', [$newReviewAssignment, $reviewAssignment, $params])` before its own DB write); `handleReviewAssignmentEdit()` converts via the already-unit-tested `ReviewSubmittedEventAdapter::fromReviewAssignmentEdit()` and enqueues through the shared `v2EnqueueEvent()` step, with a try/catch ensuring a v2 enqueue failure can never break a real review-assignment edit. Per the directive's "move new event types directly to v2" instruction, this has no v1/`dispatchEvent()`/`apiQueue` involvement at all (v1 never had a review event). Evidence tier: structural/source (`tests/v2/evt-019-review-submitted-hook-wiring.php`) plus **real live Dell acceptance (2026-09-02):** using OJS's real "Log In As" admin feature (no password ever touched) to act as the real assigned reviewer `t-reviewer1` on real submission #17, accepted the real review request and submitted a real review with a real "Accept Submission" recommendation through the actual 4-step reviewer UI. This produced a real `submission.review_submitted` queue row (id 8) at the exact real hook-fire time — confirming `handleReviewAssignmentEdit()` correctly reaches the bridge and enqueues even via a real editorial-page request (not a REST/MCP request), which matters because that request path never calls `resolve($request)` itself and would have hit EVT-023's bug pre-fix. The real scheduler then marked it `delivered` with zero live Chatwoot calls, exactly matching the deliberate "record-only, not yet allowlisted" design (`submission.review_submitted` intentionally stays off `LIVE_DELIVERY_ALLOWLIST` — same posture as the 7 other not-yet-transferred types). `submission.review_submitted` becoming a genuinely live-delivering type is EVT-016B/EVT-020 scope, not this task's.
- [x] **EVT-020 — Audit all event families against real hooks/provider contracts; drive EVT-016B's remaining per-type ownership transfers.** **Complete 2026-09-03 for all 8 known event families — see EVT-016B above for the full live-acceptance record.** Review invited/overdue/completed, revision received, payment/waiver/contributor/provider events one layer outward remain open, separate future scope — build only where a stable OJS or verified sibling-provider contract exists, never invent DAO interception merely to satisfy EVT-008/009.
  - **`submission.review_submitted` added to `LIVE_DELIVERY_ALLOWLIST` — live-accepted 2026-09-02.** Needs no ownership-switch entry: v1 never had a review event (EVT-007), so there is no `dispatchEvent()` call to retire and no double-delivery risk — v2's queue is definitionally the only possible live deliverer for this type. `tests/v2/evt-016-no-double-delivery.php` updated: this type now reaches the null-bridge guard and fatals (proving it attempts delivery), the other 6 still-v1-owned types remain blocked no-ops. **Real live Dell acceptance:** assigned real reviewer `t-reviewer1` to a real review round on real submission #18 (via the editorial UI, a real reviewer-grid AJAX call — confirmed with `review_assignments` row id 2), used the real "Log In As" feature (no password touched) to act as that reviewer, submitted a real review with a real "Accept Submission" recommendation. Produced a real `submission.review_submitted` queue row with a real, non-null `correlation_id`. `eventSyncMode` temporarily and reversibly flipped to `open_update` for the test window only (restored to the real production `note` value immediately after, verified via a direct setting read) so the real production journal's `note`-mode "never create a new contact" behavior didn't mask the proof — the real scheduler then delivered the row, creating a real new Chatwoot contact (`k.mensah@example.com`, contact id 11786) and a real audit-log row carrying the exact same `correlation_id` as the queue row (`event_delivery:submission.review_submitted`, `decision=allow`, `reason=delivered`). Ordinary OJS pages confirmed HTTP 200 throughout. **This is EVT-016B's 2nd of 8 transferred/live-accepted types.**
  - `submission.decision_recorded` also transferred and live-accepted this pass — see above (3rd of 8).
  - **`submission.revision_requested`/`submission.accepted`/`submission.rejected` transferred and live-accepted 2026-09-03.** All three flow through the exact same `handleEditorDecision()` ownership guard `decision_recorded`'s critical fix (#179) already made per-type-correct — `mapDecisionEventKey()` returns the matching `eventRevisionRequested`/`eventAccepted`/`eventRejected` key for each real decision-code group, so transferring each was purely an allowlist + ownership-switch entry, no new guard logic. **A post-merge security review then caught a critical gap this transfer introduced** — see EVT-016B's own note on the #189 fix (`handleSubmissionStatusUpdated()`/`handlePublicationPublished()` shared the same `eventAccepted`/`eventRejected` setting keys via a second real hook that had no ownership guard, a real live double-delivery exposure closed before any real occurrence hit it). Live acceptance for all three — see EVT-016B above.
  - **`publication.scheduled`/`publication.published` transferred and live-accepted 2026-09-03.** Unlike `eventAccepted`/`eventRejected`, verified safe to transfer together before making the change (the lesson from that near-miss): exactly one real v1 call site reads either setting key (`handlePublicationPublished()`, `Publication::publish`) and exactly one v2 adapter (`PublicationStatusEventAdapter`) produces both types — no sibling hook, no repeat of the accepted/rejected mistake. `handlePublicationPublished()` already carried the `isLiveDeliveryOwnedByV2($eventKey)` guard (added defensively in the #189 fix). `tests/v2/evt-016-no-double-delivery.php` simplified: all 8 `SupportEventType`s now reach the null-bridge guard and fatal, no type remains blocked. **This completes EVT-016B for all 8 known event types — see EVT-016B above for the full live-acceptance evidence.**
- [x] **EVT-021 (new finding, discovered during EVT-017's live Dell acceptance testing) — v1's `handleSubmissionCreated()` never actually fires its delivery/enqueue logic for a real submission.** Confirmed live: two real QuickSubmit-created test articles (submission IDs 22, 23, `eventSubmissionCreated`/`retryQueueEnabled` genuinely enabled) produced zero legacy `apiQueue` jobs and zero real Chatwoot contacts, while the same two real occurrences produced two real v2 durable-queue rows with `status=delivered`. Root cause confirmed against the real pkp-lib `stable_3_5_0` source (`classes/submission/Repository.php`): the `$submission` object `Repo::submission()->add()` hands to the real `Submission::add` hook is re-fetched right after insert but *before* `currentPublicationId` is ever set on it (`edit()` saves a separate internal object, never reassigning the hook's copy) — so `PKPSubmission::getCurrentPublication()` always returns null for that exact object, and `safeGetPrimaryAuthor()`'s first step fails silently every time. This affects every real caller of `Repo::submission()->add()`, including the real REST API submission-creation endpoint (`PKPSubmissionController.php`) the modern OJS 3.5 author submission wizard uses internally — not merely a QuickSubmit quirk. v2's `SubmissionCreatedEventAdapter` is unaffected by design: it defers author resolution to scheduled delivery time, after the submission has been reloaded from the database with `currentPublicationId` genuinely set. Documented in code at `safeGetPrimaryAuthor()` and asserted by `tests/v2/evt-021-v1-submission-created-hook-timing.php`. Not itself a regression to fix (v1's synchronous path failing closed here is not a double-delivery risk — see EVT-016A) — recorded so it is never mistaken for working, and so no future EVT-0xx wiring assumes v1's synchronous author resolution is reliable at hook time.
- [x] **EVT-022 (new finding, discovered during EVT-017's live Dell re-acceptance) — real author-resolution fallback silently discarded a real, present author on every real OJS 3.5 submission.** `Ojs35CompatibilityAdapter::getPrimarySubmissionAuthor()` and v1's `safeGetPrimaryAuthor()` both gated their `getData('authors')` fallback on `is_array($authors)`, which is always false on real OJS 3.5 (`Publication::getData('authors')` returns a real `Illuminate\Support\LazyCollection`, confirmed live via `get_class()`). Fixed via `is_iterable()` + a first-element loop in both copies. Independent of EVT-021's timing defect — this one fires whenever `getPrimaryAuthor()` returns null (any publication without an explicit `primaryContactId`), which is essentially every real submission, so it silently degraded the still-v1-owned event types (decision/status/publication) too, not only `submission.created`. Evidence: live-confirmed via direct dell debug scripts before and after the fix (before: `getPrimarySubmissionAuthor()` returned null for a submission with a real, verified author; after: returned the correct `{email, name, userId}`); covered by the existing `tests/v2/*` structural suite (no regression) — a dedicated behavioral test is a follow-up.
- [x] **EVT-023 (new finding, discovered during EVT-017's live Dell re-acceptance; the real root cause behind "nothing has ever actually delivered") — `RuntimeContextBridge`'s kernel was never initialized for any request-less caller.** `$kernel` was only ever set as a side effect of `resolve($request, ...)`; all 34 other methods on the bridge (`loadSubmission()`, `getPrimarySubmissionAuthor()`, everything) gated on a bare `!$this->kernel` check with no way to populate it themselves. Every scheduled task (`DeliverQueuedSupportEventsTask` confirmed live; by the same code path, `CaptainSyncScheduledTask`, `PurgeExpiredSupportDataTask`, `ProcessLegacyRetryQueueScheduledTask` wherever they touch the bridge) builds a fresh bridge and calls it directly, never through `resolve($request)` — so `$kernel` was always null and every bridge method returned null/false/empty silently, forever, regardless of how correct the surrounding pipeline was. This is the actual reason `status=delivered` on the v2 queue never once meant a real Chatwoot delivery for any event type before this fix — EVT-016A's empty allowlist and EVT-021/EVT-022's author-resolution bugs were all real, but this was the deepest, most fundamental blocker underneath all of them. Fixed by extracting the request-independent half of `resolve()` (version detection + kernel construction) into a lazy, idempotent `ensureKernel(): bool`, and replacing all 34 `!$this->kernel` guards with it — `resolve()`'s own request-driven behavior is unchanged for real REST/MCP callers, this only fixes callers that never had a request to give it. Evidence: live-confirmed on dell — before the fix, `deliverQueuedSupportEvents()`'s scheduled run completed in ~10ms (a no-op, `loadSubmission()` returning null immediately); after, the same real event's delivery took ~2s (real network round-trips) and produced a real Chatwoot conversation. Structural coverage is a follow-up (the existing suite's plugin-construction tests don't exercise this request-less path); the live acceptance run above is the real proof for now.

### CWO — Widget/render architecture hardening

- [ ] **CWO-014 — Refactor the widget hook into a router-aware, side-effect-free projection boundary.** TST-020/#148 fixed the immediate `PKPComponentRouter` crash, but the broader legacy hook still mixes router inspection, context fallback, role/privacy logic, OJS repository queries, inline JS generation, queue work, and injection. Extract a `WidgetContextService`/projection boundary that explicitly supports page vs component/AJAX renders and fails closed without leaking a fallback journal.
- [ ] **CWO-015 — Prove component/AJAX/admin render safety.** Exercise the widget hook under `PKPPageRouter`, `PKPComponentRouter`, settings modal/component renders, authenticated workflow pages, anonymous frontend pages, and no-context/site-level pages. No crash, no cross-journal fallback, no duplicate widget, no network/queue side effect.
- [x] **CWO-016 — Replace role-wide reviewer masking with resource/relationship-aware privacy.** `classes/v2/Relationship/ReviewerMaskingPolicy.php` + `CurrentSubmissionResolver.php`, wired into `ChatwootIntegrationBasePlugin::addChatwootWidget()`: masking now derives from the current resource (best-effort resolved from the request) + the same live `SubmissionRelationshipResolver`/`OjsSubmissionRelationshipEvidenceProvider` workflow-stage/review-assignment evidence RequiredActionMapper already uses, not merely the journal-level Reviewer role. The journal-wide role check is preserved only as the fail-closed default whenever no resource is resolvable (dashboard/generic pages) or resolution fails (cross-journal, unauthenticated) — it can never be *relaxed* without a proven resource-scoped author/editorial/manager/site_admin relationship, so a wrong/coincidental resolved submission id can only ever fail closed (stay masked), never leak. Evidence tier: unit (`tests/v2/pol-011-resource-aware-reviewer-masking.php` covers the exact author-on-A/reviewer-on-B multi-role scenario, no-resource fallback, no-relationship-at-all fail-closed, cross-journal fail-closed, non-reviewer never-masked, and guest fail-closed) plus structural source-wiring assertions. **Real-browser acceptance on live OJS 3.5 remains open as POL-015** — this closes the runtime/policy defect, not the browser-acceptance evidence tier.

### MIG — Post-2.0 migration lifecycle

- [ ] **MIG-001 — Introduce additive/versioned post-2.0 migrations (CRITICAL).** FND-009's in-place pre-release migration policy ceased to be valid once `v2.0.0.0` was published/installed. Never modify the initial 2.0 schema and assume deployed installs will rerun it. Add a real ordered migration chain for every future table/column/index/default change.
- [ ] **MIG-002 — Prove `2.0.0.0 → current` upgrade.** Start from the immutable 2.0.0.0 schema/package, apply the new migration chain through OJS's real plugin-upgrade mechanism, verify data preservation/idempotency, and prove no destructive downgrade/reinstall assumption is required.
- [ ] **MIG-003 — Require migration coverage for new FAQ/staff/payment/health schema.** KNO-011 cache, staff-plane persistence, portfolio/provider state, scheduler heartbeat, retention metadata, or any other new persistence must land through additive migrations plus MySQL/MariaDB/PostgreSQL coverage where supported.

### MAIL — Verification/mail completion and safety

- [ ] **MAIL-001 — Fix verification-email output encoding (HIGH).** `VerificationEmailContentBuilder` currently interpolates journal-controlled text such as journal name into HTML while only the verification URL is explicitly escaped. Apply context-appropriate HTML escaping to every variable and add hostile journal-name/subject/body tests.
- [ ] **MAIL-002 — Promote PIN and secure-link messages to native OJS EmailTemplates.** Implement the real OJS 3.5 `Repo::emailTemplate()` + `Configurable`/`Recipient` pattern researched under ADM-006, with additive migration/default-template registration and no core registry modification.
- [ ] **MAIL-003 — Enforce a strict verification-template variable allowlist.** Allowed variables are narrowly limited to journal name, verification code, expiry, and verification URL as appropriate. Never expose submission title/id, reviewer identity, relationship evidence, Chatwoot account/contact/conversation IDs, support session IDs, tokens, peppers, secrets, or raw exceptions.
- [ ] **MAIL-004 — Prevent security/internal templates from becoming Chatwoot canned responses.** Audit legacy `syncEmailTemplates()` and replace any broad/all-template sync with an explicit support-safe allowlist/classification. Password reset, verification, editorial/internal/security and other sensitive templates must be excluded by default.
- [ ] **MAIL-005 — Add real OJS/Mailpit end-to-end acceptance.** Exercise PIN email, secure-link email, and admin test email through the live OJS transport into Mailpit/safe test SMTP; verify sender/journal/locale/template variables/HTML and link behavior without claiming external inbox delivery.
- [ ] **MAIL-006 — Add header/subject and failure-semantics tests.** Reject/sanitize CRLF/header injection; define what happens when challenge creation succeeds but `Mail::send()` fails; preserve anti-enumeration while ensuring unusable challenges do not create unsafe/stale behavior.
- [ ] **MAIL-007 — Make admin mail-test wording evidence-accurate.** UI success must say the message was handed to the configured OJS mail transport, not delivered to an inbox. Surface a safe failure code and timestamp, never raw transport exceptions.

### ADM — Settings/admin-console completion

- [ ] **ADM-007 — Create one canonical settings schema.** Define key, scope, type, default, secret classification, validation, UI section/tab, export/import policy, runtime owner, and deprecation state once; drive load/save/mask/validate/export/import/render grouping from it so settings cannot silently exist in runtime but disappear from UI.
- [ ] **ADM-008 — Rebuild the Support Gateway settings console as a finished admin surface.** Split the monolithic form into coherent tabs/sections (Connection, Support API, MCP, Verification, Capability/Policy, Captain, Knowledge, Event Bridge, Queue, Providers/Payments, Health), use dedicated scoped JS/CSS assets rather than a growing inline script, replace `alert()` operational output with PKP in-place/status components, and preserve accessibility/keyboard behavior.
- [ ] **ADM-009 — Fix invalid/repeated DOM IDs and template hygiene.** `settingsForm.tpl` repeatedly emits `id="description"`; ensure unique IDs/ARIA relationships and validate rendered HTML under the real OJS admin theme/component router.
- [ ] **ADM-010 — Reconcile every runtime setting against UI and behavior.** At minimum verify `chatwootCaptainAssistantId`, `payment_support`, MCP/support tokens, Captain IDs, Event Bridge settings, verification pepper/config, widget visibility/performance settings, and every Airix/provider policy switch. A setting used by runtime must be intentionally UI-configurable or explicitly documented as internal-only.
- [ ] **ADM-011 — Decide which verification/session security parameters are admin-configurable.** Current verification TTL/cooldown/request ceilings/max attempts and support-session binding/idle/absolute TTLs are constructor defaults. Explicitly classify each as security policy vs safe admin control; expose only the latter with validated ranges and fail-closed defaults.
- [x] **ADM-012 — Resolve the dead `launcherBottomOffset` setting.** Resolved by removal, not wiring: no Product Bible requirement names a launcher position/offset control (verified against `PRODUCT_BIBLE.md` directly — its only "launcher" reference is the unrelated "contextual launcher intent" concept). Removed from `ChatwootSettingsForm.php` (`initData`/`readInputData`/`execute`), `templates/settingsForm.tpl`, and `locale/en/locale.po`. It was never in `EXPORT_KEYS`/`LEGACY_EXPORT_KEYS`, so import/export compatibility is unaffected; existing stored values on old installs become a harmless orphaned `plugin_settings` row (never read again, no migration needed). Evidence tier: structural/source (`tests/v2/adm-012-launcher-offset-removed.php` proves the key is gone from every real runtime location). `docs/v2/V1_INVENTORY.md` and `docs/v2/SETTINGS_RECONCILIATION.md` updated in the same change.

### AUD — Health, audit, correlation, retention, logging

- [ ] **AUD-009 — Separate “configured” from “healthy”.** The current local aggregator is intentionally cheap but `configured=true` must not be represented as live health. Track/display configured state separately from last verified health, last verified timestamp, cached current state, safe last failure code, and an explicit live-check action where appropriate.
- [ ] **AUD-010 — Add scheduler execution evidence/heartbeat.** Task registration does not prove cron is running. Track the last successful/failed execution for purge, event delivery, Captain sync, FAQ sync, and other scheduled jobs; surface stale/missing execution without synthetic healthy status.
- [ ] **AUD-011 — Expand safe component/provider/queue health detail.** Close AUD-008/KNO-020 with provider-level knowledge facts/exclusions/conflicts/fingerprint/routes/staleness, Captain resource state, event pending/retry/dead-letter counts and safe timing/error codes, payment/provider applicability, verification/mail/API/MCP status, and no raw payload/exception leakage.
- [ ] **AUD-012 — Expand the persisted audit model safely.** Where permitted, record plane, operation/tool, capability, safe resource type/id, decision, reason code, result class, and correlation ID. Continue excluding email, reviewer identity, OTP/link tokens, manuscript text, relationship evidence, secrets, and raw exceptions.
- [x] **AUD-013 — Correlation propagation across the event/queue/delivery chain.** Closes the "OJS event → durable queue → Chatwoot delivery → delivery/audit record" half AUD-002 left open. Added a `correlation_id` column to the v2 event queue table (idempotent add-if-missing on `upEventQueue()` — a bare `Schema::hasTable()` guard alone would never reach an already-installed real table on upgrade, only fresh installs); `v2EnqueueEvent()` generates a real `CorrelationId::generate()` for every OJS-hook-driven enqueue (there is no inbound request to reuse one from, unlike the REST/MCP side) and the repository persists it on the row; `deliverQueuedSupportEvents()` now records a real audit entry for every outcome (delivered/delivery_failed/internal_error) via a new `v2AuditEventDelivery()` helper, reusing the exact same persisted `DatabaseSupportApiAuditLogger` sink AUD-001/verification auditing already uses (endpoint `event_delivery:{type}`, distinguishable from a verification-endpoint entry in the same shared log) — never a second, independently-maintained audit table. A legacy row enqueued before this column existed gets a fresh correlation ID generated at delivery time rather than an empty one, since the audit sink silently refuses to persist a blank `correlationId`. Evidence: `tests/v2/aud-013-correlation-propagation.php` (structural source assertions for the migration/repository/plugin wiring, plus a real SQLite proof that a correlation ID round-trips through the real schema shape and that the real upgrade path — `ALTER TABLE ... ADD COLUMN` on a table with pre-existing rows — succeeds without disturbing them). **Live Dell acceptance (2026-09-02):** the real migration was run against the real production table on dell (`ALTER TABLE` applied live, all 9 pre-existing rows preserved with `correlation_id = NULL`, confirmed via `DESCRIBE`); a real editorial "Accept Submission" decision on real submission #17 produced a real `submission.accepted` queue row with a real, non-null `correlation_id`; the next real scheduler tick delivered it and wrote a real audit-log row — **the exact same `correlation_id` value appears on both the queue row and the audit-log entry** (`event_delivery:submission.accepted`, `decision=allow`, `reason=delivered`), proving the full real chain end to end, not just structurally. **Captain/admin-action propagation closed 2026-09-03:** the legacy `apiQueue` dead-letter give-up path (`ChatwootIntegrationBasePlugin::processApiQueue()`) had never had a correlation ID at all and was still writing only to the AUD-001 placeholder `ErrorLogSupportApiAuditLogger` — the one call site left behind when every other outcome moved to the real persisted sink. Switched to `DatabaseSupportApiAuditLogger` with a fresh correlation ID generated at give-up time (a legacy job never had one to inherit). `CaptainSyncScheduledTask` (runs entirely outside the event/queue/delivery chain — it never enqueues a `SupportEvent`) now records one real audit entry per journal per sync run via a new `v2AuditCaptainSync()`, the same granularity the task's own execution-log line already uses. **Real live Dell acceptance (2026-09-03):** invoked the real `CaptainSyncScheduledTask::execute()` against the real production journal (`ajdsi`) via the real plugin instance (not a mock) — produced a real audit-log row (`id=61`, `endpoint=captain_sync`, `decision=allow`, `reason=documents=1:tools=12:scenarios=5`, a real freshly-generated `correlation_id`). Dead-letter give-up's own live re-verification (a real legacy job forced to exhaust retries) is structural-evidence-only for now — dell's legacy `apiQueue` inventory has been empty since EVT-017's own inventory pass, so there is no real non-empty legacy queue to exercise this against without first manufacturing one; the safe-fields/no-PII behavior and the real sink/correlation-ID wiring are covered by `tests/v2/evt-017-legacy-queue-inventory-and-ownership.php`.
- [ ] **AUD-014 — Complete repository-wide log redaction.** AUD-006 currently proves one audit sink only. Inventory every `error_log`/exception/log call (including legacy `$e->getMessage()` patterns), replace raw exception/user payload logging with safe codes/context, and add secret/PII regression tests for all sinks.
- [ ] **AUD-015 — Define/purge all persistence lifecycles.** Add retention for `chatwoot_support_event_queue`, `chatwoot_support_knowledge_sync`, FAQ cache/new provider state, and the legacy `apiQueue` until retired. Define terminal-row retention and safe purge semantics; document and test uninstall behavior.

### SEC — Additional robustness/security closure

- [ ] **SEC-011 — Harden or replace the regex HTML sanitizer.** `KnowledgeSanitizer` is home-grown regex sanitization. Either use a vetted sanitizer already available in OJS/PKP or run a hostile corpus covering malformed tags/quotes, mixed case, HTML entities, encoded/obfuscated URL schemes, namespace tricks, nested markup, and attribute edge cases. Do not claim exploitability without evidence, but do not treat simple happy-path tests as sufficient.
- [ ] **SEC-012 — Verify the real primary ingress rate limiter/proxy configuration.** API-007 documents that APCu is defense-in-depth and the real ceiling belongs at nginx/Apache/Cloudflare/LB. Verify that the Dell/live deployment actually enforces the intended limits and trusted-forwarded-protocol model; documentation alone is not evidence.
- [ ] **SEC-013 — Replace best-effort APCu escalation idempotency with durable idempotency where justified.** `EscalationIdempotencyGuard` is per-worker/fail-open. Add a small DB-backed reservation/ledger so simultaneous workers cannot create duplicate handoff notes; preserve safe retries and correlation.
- [ ] **SEC-014 — Re-run admin-action CSRF/auth boundary tests after the console refactor.** Cover old and new settings verbs, Captain repair, queue retry, test mail, imports/exports/global defaults, future provider controls, and ensure no privilege expansion through component/AJAX routes.
- [ ] **SEC-015 — Perform a new final security review after all corrective/completion work.** SEC-010 covered the old release commit, not the post-release fixes/new provider/staff/mail/migration work. Re-run static analysis, secret scans, IDOR/replay/brute-force/injection/privacy tests and real-runtime abuse cases against the actual next candidate.

### MCP / staff-plane completion

- [x] **MCP-010 — Implement `server/discover`.** Registered in `mcpRequest()`: reports the real `McpProtocol::REVISION` and `McpProtocol::SUPPORTED_METHODS` (never an independently-maintained duplicate that could drift), plus the same real tool/resource lists `tools/list`/`resources/list` already expose (`$registry->list()`/`$resourceRegistry->list()`, not a second parallel description). Evidence tier: unit (`tests/v2/mcp-010-server-discover.php` exercises a real `McpDispatcher`/`McpRequest` round trip and asserts the response shape/content) plus source-wiring assertions on the real registration call. Real-client acceptance against the live MCP gateway on dell not yet separately re-run — covered by the existing MCP acceptance surface the next time it runs.
- [x] **MCP-011 — Implement `identity.request_verification`.** Registered in `mcpRequest()`: authenticates the MCP caller through the same shared `v2ResolveMcpIdentity()`/`SupportApiRequestResolver` every other MCP tool uses, then reuses the exact real REST pipeline verbatim — `VerificationChallenge::PURPOSES` validation, `RuntimeContextBridge::requestVerificationChallenge()`, `VerificationEmailContentBuilder`, `Mail::send(new SupportVerificationMailable(...))`, the shared `v2AuditVerificationEvent()` sink, and `ResponseTimingNormalizer::normalize()` at the same timing floor — never a second, independently-implemented verification engine. Structurally identical anti-enumeration guarantee to REST: exactly one return statement for the generic `{verificationRequested: true, challenge: "..."}` shape, regardless of whether the account exists, mail fails, or the request is throttled. Evidence tier: structural/source (`tests/v2/mcp-tools.php` — extended this pass — proves the real registration, the shared pipeline reuse, and the single-return-path anti-enumeration guarantee). Real-client acceptance against the live MCP gateway on dell attempted 2026-09-03 but blocked — see MCP-015 (an infra-level Authorization-header routing gap, not an application defect).
- [x] **MCP-012 — Implement `identity.confirm_verification`.** Registered in `mcpRequest()`: same MCP-caller authentication as MCP-011, then reuses the exact real `RuntimeContextBridge::confirmVerificationPin()` (the same atomic challenge-consumption engine REST uses — never a second PIN check) and `establishSupportSessionFromExternalVerification()`. Collapses every failure reason into the same generic `{verified: false}` result and never returns the stored secret, same as REST. Evidence tier: structural/source (`tests/v2/mcp-tools.php`, extended this pass). Real-client acceptance against the live MCP gateway on dell attempted 2026-09-03 but blocked — see MCP-015 (an infra-level Authorization-header routing gap, not an application defect).
- [ ] **MCP-013 — Make an evidence-based final decision on `submission.get_timeline`.** Inventory real OJS 3.5 timestamps/events first. Build only a timeline that can be directly sourced without invented editorial state; otherwise record a permanent ADR-backed N/A/deferred decision.
- [ ] **MCP-015 (new finding, 2026-09-03) — Real-client acceptance for MCP-011/012 against the live dell gateway is blocked by an infra-level Authorization-header gap, not an application bug.** Attempted the real live-client acceptance run (a real `tools/call` POST to `https://ojs-demo.airixmedia.com/ajdsi/ojsMcpGateway` for `identity.request_verification`, with a real, freshly-generated `mcpServiceToken` configured and real Chatwoot account/contact/conversation IDs from this session's earlier live tests). The real request reached `mcpRequest()` (HTTP 200, a real JSON-RPC error body) but failed `McpAuthenticator::verify()` with "Request could not be authenticated." Isolated the cause with a controlled diagnostic: a plain PHP file placed directly in the web root confirmed `$_SERVER['HTTP_AUTHORIZATION']` is null on this Apache config (expected — Apache doesn't forward it to CGI-style env vars by default) but `getallheaders()`/`ServiceTokenAuthenticator::resolveAuthorizationHeader()`'s own fallback correctly recovers it, and `ServiceTokenAuthenticator::verify()` correctly validates a real token, **when the request is served directly by Apache outside OJS's own front-controller/mod_rewrite dispatch.** The exact same header is not resolving once the request is routed through OJS's real page-handler dispatch (`LoadHandler` → `McpGatewayPageHandler`) in production — a real Apache/mod_rewrite passthrough gap specific to this route shape, the same general family of deployment-config surprise `McpGatewayPageHandler`'s own docblock already documents for `PageHandler::authorize()` (see TST-014), not a defect in `ServiceTokenAuthenticator`/`McpAuthenticator` themselves (both verified correct in isolation). The test `mcpServiceToken` was restored to its original empty value afterward — MCP was not previously configured on this journal at all, so nothing regressed. **Do not attempt to fix this by editing production Apache/vhost config from an unattended session** — it needs either a `SetEnvIf`/`RewriteRule`-level fix reviewed by the owner, or confirmation of the real deployment's web-server front end, neither safe to guess at autonomously. Real live-client acceptance for MCP-011/012 remains blocked on this until resolved.
- [ ] **POL-014 — Implement a real read-only staff plane foundation.** POL-004/007 and DIA-012 remain open: create a separate staff credential/namespace, derive authorization from live OJS staff roles/relationships, define read-only staff capabilities/serializers/audit context, and prove public credentials/tools cannot cross into it. Do not add staff mutations/refunds/deadline changes in this first slice.
- [ ] **MCP-014 — Implement a genuinely separate staff MCP namespace/credential only after POL-014.** Reopen the substance of MCP-005: a reserved `CONSUMER_MCP_STAFF` constant and proof that public MCP cannot reach staff capabilities is a security test, not implementation of a staff namespace.

### PRV / Payment Portfolio / Airix integration completion

- [ ] **PRV-008 — Implement `AccountSupportProviderInterface` with a real Magic Login adapter.** Verify the installed sibling's public/versioned contract, expose only safe availability/diagnostic facts, and keep Magic Login session/token semantics separate from Support Gateway verification.
- [ ] **PRV-009 — Implement `SubmissionRequirementProviderInterface` with Required Submission Files.** Reuse the verified sibling contract already used by diagnostics; add version/applicability/health and no direct table coupling.
- [ ] **PRV-010 — Implement `ContributorSupportProviderInterface` with Contributor User Sync.** Expose only the verified caller/submission-safe contributor support facts, never another contributor's data and never auto-merge accounts.
- [ ] **PRV-011 — Add Diagnostic/Event provider facets only with a real first implementation.** Pair every new contract with an installed verified sibling that needs it; do not create empty interfaces for architectural decoration.
- [ ] **PRV-012 — Generalize provider version constraints/conflict declarations.** Replace one-off hard-coded compatibility checks with a small explicit supported-version mechanism and deterministic overlap/conflict policy.
- [ ] **PAY-010 — Build the canonical Payment Portfolio model.** Implement the Product Bible pipeline: fee producer → obligation → waiver/adjustment → payable balance → collector/gateway → ledger → refund/dispute/reconciliation, while preserving the public plane as read-only.
- [ ] **PAY-011 — Integrate the verified installed payment siblings.** Inspect and adapt real supported versions of Bachs, Paystack, Request Waiver, Paymethod Support and other confirmed gateways/orchestrators from the Airix supplement; no guessed APIs and no direct table coupling where a public helper/service exists.
- [ ] **PAY-012 — Add `payment_support` as a safe explicit journal policy control.** It currently defaults false and can make implemented payment status unreachable. Expose the real policy switch with safe defaults/help/health, without enabling money mutations.
- [ ] **PAY-013 — Prove public support can never mutate money state.** Regression-test that Captain/REST/MCP public credentials cannot mark paid, approve a waiver, refund, alter gateway data or call browser/CSRF payment handlers; future mutations remain staff-only with server-derived parameters, idempotency and audit.

### KNO / state / privacy completion

- [ ] **KNO-021 — Build the approved FAQ local-sync/cache provider.** Close KNO-011 with scheduled Chatwoot `Captain::AssistantResponse` sync → journal/assistant-isolated local cache → sanitized `KnowledgeProvider`; approved-only, lower precedence than structured/official facts, stale local fallback on Chatwoot outage, and never live-fetch on anonymous knowledge-page requests.
- [ ] **KNO-022 — Complete provider-level Knowledge health drill-down.** Close KNO-020 with provider status, facts emitted, private/unsupported facts excluded, safe conflicts/winner source, fingerprint, generated routes, FAQ cache age/staleness and Captain sync relationship without exposing losing private values.
- [ ] **STA-010 — Research and expand support-language states only where OJS 3.5 evidence exists.** Investigate `awaiting editor assignment`, `awaiting reviewer assignment`, `reviewers invited`, `awaiting reviewer reports`, `editor assessing reviews`, `revision received` and similar support-facing distinctions against real OJS fields/repositories. Add only directly provable states; otherwise narrow the Product Bible rather than infer. Add the dedicated `SupportStateMapper` unit suite required by TST-001.
- [ ] **POL-015 — Add real multi-role/blind-review widget acceptance.** Beyond serializer tests, verify browser/projected Chatwoot identity/custom attributes for users holding multiple journal/resource roles across different submissions, with no reviewer identity/existence leakage.

### ARC — Incremental code-structure hardening

- [ ] **ARC-001 — Reduce `ChatwootIntegrationV2Plugin` orchestration concentration incrementally.** Do not rewrite the plugin, but as affected areas change extract testable boundaries for Support Gateway request orchestration, MCP gateway, verification, widget context, Event Bridge, Captain sync, admin health, and settings. The goal is to stop needing source-string assertions merely because every integration path lives in one giant plugin class.
- [ ] **ARC-002 — Keep the root convention class minimal.** Preserve TST-014's required `ChatwootIntegrationPlugin extends ChatwootIntegrationV2Plugin` shell and add a regression preventing behavior from creeping back into the convention wrapper.

### ACC — Real acceptance / package / database closure

- [x] **ACC-001 — Full REST/OpenAPI response contract suite (success-envelope coverage).** `tests/v2/openapi-contract.php` now bidirectionally checks all 14 REST operations' success-response schemas against real serializer/handler output (not hand-written fixtures — the real `SupportIdentitySerializer`/`SubmissionVerificationSerializer`/`SubmissionListSerializer`/`SubmissionSupportSerializer`/`RequiredActionsSerializer`/`PublicationStatusSerializer`/`PaymentStatusSerializer`/`DiagnosticResultSerializer`/`HandoffSummaryFormatter`, plus literal fixtures matching the exact inline shapes for the four endpoints with no dedicated serializer — `/status`, `/verificationRequest`, `/verificationConfirm`, and `/escalate`'s own top-level envelope): both directions checked (an undeclared key the code emits, and a declared schema key the code never emits, are both a real contract drift and fail the test). **Not yet closed**: error envelopes (`ErrorEnvelope`, 400/401/403/404/429/500 paths) are not yet contract-tested per operation — that remains open scope, tracked as ACC-001B below, so TST-013/API-018 stay marked partial in CMP-003 until that lands too.
- [x] **ACC-001B — Error-envelope contract coverage.** `docs/v2/openapi.json` now documents the real full error surface per operation: 400/401/429/500 for all 14 (every operation shares `resolveSupportApiRequest()`'s failure paths, which really emit all four — confirmed by grepping every real `new SupportApiFailure(...)`/`SupportApiResponse::error(...)` call site, not assumed), plus 403 for `/escalate` specifically (the one operation that actually emits `CAPABILITY_DENIED`). `tests/v2/openapi-contract.php` extended: a real subprocess call to `SupportApiResponse::error()` (it calls `exit()` unconditionally, so it cannot run in-process without ending the test) captures the real emitted JSON and checks it against `ErrorEnvelope`'s declared shape, bidirectionally, same discipline as the success-envelope checks; every real `SupportApiErrorCode` constant is cross-checked against the schema's documented code list; every operation's declared response-code set is cross-checked against the real status codes its shared failure path and (for escalate) its own extra error call can actually emit. **TST-013/API-018 now close in CMP-003** — full success + error envelope coverage for all 14 operations.
- [ ] **ACC-002 — Exact candidate archive clean-install lifecycle.** Build the eventual candidate `.tar.gz` and install it on a separate clean OJS 3.5 environment as an actual package (not the bind-mounted checkout): install → enable → configure → migrations → REST/MCP/routes → scheduler → disable → re-enable → uninstall/reinstall where safe.
- [ ] **ACC-003 — Real two-journal end-to-end isolation matrix.** Journal A/B must independently prove settings, sessions, verification, mail/template sender+locale, Knowledge/FAQ cache, Captain, providers/payment, events/queues, REST/MCP, audit and widget masking. TST-009's ContextResolver slice alone is insufficient.
- [ ] **ACC-004 — Complete supported database matrix.** Preserve MySQL 8 proof, add MariaDB and PostgreSQL where officially supported/practical, with real migrations, atomic challenge consume, event idempotency/queue backoff, audit, additive upgrade migrations and provider/cache schema.
- [ ] **ACC-005 — Real scheduler execution matrix.** Prove registered tasks actually run under the deployment scheduler and update expected state for purge, event delivery, Captain sync and FAQ sync; include outage/recovery and stale-heartbeat detection.
- [ ] **ACC-006 — Real Chatwoot/Captain end-to-end matrix.** Exercise contact/conversation lookup/create, private note, opt-in customer message, escalation idempotency, Captain document/tool/scenario create-sync-conflict-recovery, and prove unrelated resources are untouched.
- [ ] **ACC-007 — Real verification matrix.** Logged-in silent V2, PIN, secure link, resend, expiry, lockout, rate limit, replay, cross-journal/cross-conversation binding, mail failure and browser-link completion through the live OJS/Chatwoot/Mailpit stack.
- [ ] **ACC-008 — Re-run the complete Dell acceptance matrix from the top after the corrective backlog lands.** Treat every previously-passed row as potentially invalidated by architectural/settings/mail/provider/migration changes; record exact build SHA, OJS/PHP/DB versions and external-service versions for the final candidate.

---

## FND — Foundation

- [x] **FND-001** Add root GPL-compatible `LICENSE`. — root `LICENSE` (GPL-3.0-or-later, SPDX-tagged).
- [x] **FND-002** Add root `SECURITY.md` and private vulnerability process.
- [x] **FND-003** Add `CONTRIBUTING.md` and v2 branch/review conventions.
- [x] **FND-004** Inventory all v1 settings and classify keep/migrate/deprecate/remove. `docs/v2/V1_INVENTORY.md` now lists every real setting individually (including each `hideForRole_*` key by its real role ID, and the v2-added settings alongside for one complete current inventory), not grouped prose. **A real, previously-undocumented bug was found while building this literal table**: `launcherBottomOffset` is saved/loaded by the settings form and rendered as a real input field, but is never read anywhere in `addChatwootWidget()`'s actual widget-script assembly — verified by source, it has zero real effect today. `docs/v2/CORE_BRIDGE_GUIDE.md` §5 (merged in DOC-002, PR #127) incorrectly claimed it had a real effect; corrected in the same PR as this finding. Fixing the dead setting itself (remove vs. wire up) is left as a genuine open follow-up, not resolved here — this pass is inventory-and-correction, not a behavior change.
- [x] **FND-005** Inventory all v1 hooks against supported OJS versions. Every one of v1's 7 registered hooks individually verified against a real local `pkp-lib` `stable-3_5_0` checkout with exact file/line citations (`docs/v2/V1_INVENTORY.md`) — all real, all currently firing on OJS 3.5. Confirmed `Publication::publish` and the similarly-named but distinct `Publication::publish::before` are two real, different hooks at the same call site; v1 correctly uses the post-publish one. Scope is OJS 3.5 only, matching this release's real target — OJS 3.6 is explicitly out of scope and not verified here.
- [x] **FND-006** Baseline v1 widget/API/event regression tests (retry-queue slice). `tests/v2/live-plugin.php` already covered widget-rendering fallback and settings export/redaction; new `tests/v2/fnd-006-v1-retry-queue.php` closes the remaining gap — v1's own event-sync/retry-queue bookkeeping (`dispatchEvent`/`sendChatwootEvent`/`enqueueApiJob`/`processApiQueue`), exercised via reflection on a real, unmodified `ChatwootIntegrationBasePlugin` instance rather than a fabricated reimplementation. Scoped the same way TST-002/003 were: `ChatwootApiService` can't be instantiated here (real Guzzle client in its constructor, no Composer/vendor tree) — the test drives the real code through its deterministic pre-`ChatwootApiService` failure guard (blank connection settings), which is enough to prove every real bookkeeping guarantee end-to-end: enqueue-as-real-JSON, the real exponential backoff formula, the real per-call `$limit` cap, and real eventual give-up at `maxRetryAttempts`. **A real, previously-undocumented behavior was found while building this**: `dispatchEvent()` opportunistically drains up to 4 already-queued jobs on every call, before handling its own new event — so any two events dispatched close together interleave retries of each other's queue backlog; this surfaced as a real test-authoring pitfall (a naive "three isolated dispatches" setup silently mutated all three jobs' attempt counts) and is now documented as a real system behavior, not fixed or worked around. A real "success" path additionally exercising `ChatwootApiService` itself remains a follow-up, needing the same real-local-HTTP-server technique TST-003 established for that specific class.
- [x] **FND-007** Define service/container structure for v2 modules. — `classes/v2/` is organized by module (`Api`, `Audit`, `Captain`, `Chatwoot`, `Compatibility`, `Context`, `Contracts`, `Diagnostics`, `Handoff`, `Http`, `Knowledge`, `Migration`, `Plugin`, `Policy`, `Provider`, `Relationship`, `Runtime`, `Session`, `Settings`, `State`, `Task`, `Verification`), composed through `SupportGatewayKernel`/`RuntimeContextBridge` rather than a DI container — no framework container exists to hook into in a PKP plugin, so a hand-wired composition root is the real service-structure equivalent here.
- [x] **FND-008** Create OJS compatibility adapter interface(s). — `OjsCompatibilityAdapterInterface` + `Ojs35CompatibilityAdapter`.
- [x] **FND-009** Build install/upgrade/uninstall migration framework. — `InstallSupportGatewayMigration` (sessions/challenges/knowledge-sync/audit-log tables), edited in place pre-release rather than versioned, per docs/v2 pre-release migration policy.
- [x] **FND-010** Remove secrets from normal settings export/default diagnostics. — `exportSettings()` strips `chatwootApiAccessToken`/`chatwootIdentityValidationSecret`/`chatwootSupportApiToken` and reports which keys were redacted rather than exporting them.
- [x] **FND-011** Add coding/static-analysis rules compatible with PKP target. — `.php-cs-fixer.php`, the exact ruleset from a real local `pkp-lib` checkout's own `.php_cs_rules` (minus its repo-internal `PKP/hookfixer` custom fixer, which has no equivalent here). Gated in CI (`.forgejo/workflows/ci.yml`) as `--dry-run` against `classes/v2`/`tests/v2` only — legacy v1 files predate this ruleset and have not been reformatted (a separate, out-of-scope change); a repo-wide gate today would fail on unrelated legacy code. The `.github/workflows/ci.yml` mirror is publication-only per house rules (never a merge gate) and was already stale before this change (it lists a narrower, outdated subset of v2 test files); not touched here.
- [x] **FND-012** Add CI skeleton for package/tests. — `.forgejo/workflows/ci.yml` (real gate for every PR merged into `v2-dev` this whole build) and a mirrored `.github/workflows/ci.yml`.

## CTX — OJS Context

- [x] **CTX-001** Implement journal/context resolver. — `ContextResolver::resolve()` → `SupportContext::contextId()`.
- [x] **CTX-002** Implement authenticated user resolver. — `SupportContext::userId()`/`isAuthenticated()`.
- [x] **CTX-003** Implement current journal role resolver. — `SupportContext::roleIds()`.
- [x] **CTX-004** Implement page/operation context DTO. — `SupportContext::page()`/`operation()`.
- [x] **CTX-005** Implement article context adapter. — `ResourceContextResolver` resolves an `article` template key to a `submission`-typed `ResourceContext` (an OJS published article is the same underlying submission, not a distinct resource kind).
- [x] **CTX-006** Implement submission/workflow context adapter. — `ResourceContextResolver::resolve()` (template/request-parameter/known-route detection, all re-authorized downstream — detection is never authorization).
- [x] **CTX-007** Implement review context adapter. — `ResourceContextResolver::fromKnownRoute()` now also detects the reviewer route (`pages/reviewer/ReviewerHandler`, operations submission/step/saveStep/showDeclineReview/saveDeclineReview/downloadFile — verified against a real local pkp-lib checkout), sourced as `known_route:reviewer`. There is no separate "review" resource type to detect: `SubmissionAccessPolicy` -> `DataObjectRequiredPolicy::getDataObjectId()` reads `args[0]` as the submission id on this route exactly like the workflow page does — OJS never exposes a reviewId/reviewRoundId in route args at this layer (review-round detail is fetched client-side via grids/API calls). Unassigned operations on the reviewer page are rejected rather than guessed.
- [x] **CTX-008** Implement payment/support intent context. — `SupportIntentResolver::resolve()`'s `payment_help` branch, wired live via `ChatwootContextProjector`.
- [x] **CTX-009** Add multi-journal isolation tests. — `tests/v2/context-resolver-isolation.php`: two requests bound to different journals (same numeric user id, different roles) resolve independently via both `resolve()` and `resolveForUser()` — journal id/path/page/operation/roles never cross-contaminate; a zero/invalid context id fails closed rather than falling back to a different journal.
- [x] **CTX-010** Add locale normalization/fallback tests. — same file: `ContextResolver` deliberately has no normalization logic of its own (real locale negotiation is `PKP\facades\Locale`'s job, done by every real caller before constructing `$locale`) — the test proves it faithfully stores whatever it's given verbatim, including the documented empty-string default, and never lets a locale leak/stick across independent `resolve()` calls.

## CWO — Chatwoot Connector

- [x] **CWO-001** Refactor Chatwoot API client behind interface. — `ChatwootApiService implements ChatwootConversationClientInterface, ChatwootCaptainClientInterface`.
- [x] **CWO-002** Preserve/verify HMAC `setUser` integration. — the v1 `setUser`/`identifier_hash` HMAC (`ChatwootIntegrationPlugin::addChatwootWidget()`) is untouched by v2 and verified to always still run: `tests/v2/live-plugin.php` asserts `parent::addChatwootWidget()` executes unconditionally even if v2's own support-session bootstrap throws.
- [x] **CWO-003** Define safe custom-attribute schema. — `classes/v2/Compatibility/WidgetCustomAttributeSchema.php`: a declarative allowlist of all 14 real `$attrs` keys `addChatwootWidget()` sends (`journal_id`/`journal_name`/`requested_page`/`requested_op`/`is_masked`/`orcid`/`affiliation`/`roles`/`active_submissions`/`context_type`/`article_title`/`article_doi`/`article_id`/`section_title`), each classified `public`/`user_derived`, plus an explicit `FORBIDDEN_KEYS` denylist for identity-shaped keys (email/password/name/user_id/token/secret/identifier_hash) that must never become a custom attribute — identity travels only through `setUser()`'s own dedicated parameters. Does not change v1's runtime behavior at all; `tests/v2/widget-custom-attribute-schema.php` checks the schema and the real legacy source bidirectionally, so either one drifting from the other fails the build.
- [x] **CWO-004** Remove any authorization dependency on custom attributes. — `tests/v2/custom-attribute-authorization-independence.php`: extracts the real, complete custom-attribute key list directly from `ChatwootIntegrationPlugin.php`'s `$attrs` assignments (`journal_id`/`roles`/`orcid`/`affiliation`/`active_submissions`/`is_masked`/etc.) and every real `getUserVar()` parameter name v2 actually reads, then asserts the two sets never intersect. A future accidental read of a custom-attribute key as a request parameter anywhere in `classes/v2/` would fail this test immediately.
- [x] **CWO-005** Implement contextual launcher intents. — `SupportIntentResolver` + `ChatwootContextProjector::project()`, wired live via `ChatwootIntegrationV2Plugin`'s template-header injection (not just built-but-unused: confirmed called from the plugin, not only from tests).
- [x] **CWO-006** Add idempotent contact/conversation lookup strategy. — `v2DeliverQueuedEventRow()` (EVT-011) always calls `findContactByEmail()` before ever creating a contact, and `getContactConversations()` before ever opening a new conversation — a contact/conversation is only ever created when a real lookup proves none exists yet, mirroring v1's real `sendChatwootEvent()` lookup-then-create order exactly.
- [x] **CWO-007** Add queued retry/dead-letter structure. — `chatwoot_support_event_queue` (EVT-011/014): `DatabaseSupportEventQueueRepository::markFailed()`'s exponential backoff (`run_after`, same shape as v1's own retry queue) plus `attempts`/`status`, with a `failed` row past `max_attempts` as the dead letter — now actually driven by the real `deliverQueuedSupportEvents()` consumer, not just schema.
- [x] **CWO-008** Detect/report Captain API feature availability where possible. `ChatwootCaptainClientInterface`'s docblock records that Captain (incl. Documents) is Enterprise-Edition-gated in self-hosted Chatwoot (verified against `chatwoot/chatwoot` `develop` `enterprise/app/controllers/api/v1/accounts/captain/documents_controller.rb`), and every call degrades to `null`/unavailable rather than a fatal — the health signal is surfaced in the admin settings form's Support Gateway Health section (ADM-002, an explicit "Captain unavailable" degraded/failed state is visible there), and the admin-action half (a manual repair/sync control) is closed by ADM-003's "Sync/Repair Captain" button — this entry was stale (written before ADM-003 existed) and is now corrected rather than left pointing at a gap that no longer exists.
- [x] **CWO-009** Implement optional Captain Document provisioning. — `CaptainDocumentProvisioner`: idempotent create-or-sync keyed on the Knowledge Compiler's own fingerprint, verified against real `chatwoot/chatwoot` `develop` routes (`config/routes.rb`: `namespace :captain do resources :documents, only: [:index,:show,:create,:destroy] do post :sync ... end end`) and the enterprise controller's actual permitted params (`name`, `external_link`, `assistant_id`). No update/PATCH exists in the real API for web-sourced documents — content changes go through `sync` (re-crawl), never a field edit, which is exactly what this provisioner does.
- [x] **CWO-010** Implement optional Captain Custom Tool provisioning. — `CaptainCustomToolProvisioner` + `CanonicalToolCatalog` (12 fixed tools, one per Support API endpoint that makes sense as an LLM-callable tool — never one per provider/journal-fact). Verified against real `chatwoot/chatwoot` `develop` source: `config/routes.rb`'s `resources :custom_tools do post :test, on: :collection end`, `enterprise/app/controllers/api/v1/accounts/captain/custom_tools_controller.rb`'s actual permitted params, `enterprise/app/models/captain/custom_tool.rb`'s `auth_type` enum (`none`/`bearer`/`basic`/`api_key`) and 15-tool account cap, and `enterprise/app/models/concerns/toolable.rb`'s Liquid template rendering (`strict_variables: true`) — which is why every canonical tool param is `required: true` rather than truly optional. Unlike Documents, Custom Tools have a real update endpoint, so a service-token rotation genuinely pushes an update rather than a no-op. **Also fixed a real integration bug found during this work**: Chatwoot's Custom Tool HTTP client always sends `Content-Type: application/json`, but this plugin's Support API only ever read `$request->getUserVar()` (PHP never populates `$_POST` for a raw JSON body) — every provisioned tool call would have seen every field as missing. Fixed with `JsonRequestBodyParser`, wired into `resolveSupportApiRequest()`.
- [x] **CWO-011** Implement optional Captain Scenario provisioning. — `CaptainScenarioProvisioner` + `CanonicalScenarioCatalog` (the 5 families already specified in `KNOWLEDGE_DIAGNOSTICS.md` §7: Journal Information, Account Support, Submission Support, Payment & Publication Support, Human Escalation). Verified against real `chatwoot/chatwoot` `develop` `enterprise/app/controllers/api/v1/accounts/captain/scenarios_controller.rb` and `enterprise/app/models/captain/scenario.rb`. Key finding: `Captain::Scenario` has a `before_save :resolve_tool_references` callback that recomputes its `tools` column by parsing `[Title](tool://slug)` markdown references out of the `instruction` text — the `tools` param the API accepts is not the actual source of truth, so this provisioner never sends it and instead embeds real tool-slug references directly in each scenario's instruction. Depends on Custom Tool provisioning having already run (a tool can only be referenced by its real, Chatwoot-assigned slug); a scenario whose required tools are not yet resolvable fails closed (`required_tool_unavailable`) rather than provisioning a broken reference — verified this can never silently create a scenario with a dangling tool link.
- [x] **CWO-012** Ensure provisioner never deletes unrelated admin resources. — `CaptainDocumentProvisioner` never calls a delete/destroy endpoint at all. Ownership is proven only by a local `CaptainSyncState` record (`chatwoot_support_knowledge_sync` table, keyed by context/locale/resource type) — a name/`external_link` match at the remote API is treated as `unmanaged_document_exists` (a conflict, recorded but never created, never adopted, never touched), exactly per the freeze directive ("never assume same name means plugin-owned").
- [x] **CWO-013** Add configuration drift/health report. `CaptainProvisioningHealthService`/`CaptainProvisioningHealthReport`/`CaptainResourceHealth` build a full drift/health snapshot across every expected Captain resource (the one Document, all 12 `CanonicalToolCatalog` tools, all 5 `CanonicalScenarioCatalog` scenarios) — `owned`/`degraded`/`conflict`/`failed`/`not_provisioned` per resource, deterministic overall state. Deliberately a pure read over the local `chatwoot_support_knowledge_sync` records already written during provisioning — never a Chatwoot API call itself, so it is always cheap/safe to build. Surfaced in the admin settings form's Support Gateway Health section (ADM-002); the manual "Sync/Repair Captain" action is closed by ADM-003 — this entry was stale (written before ADM-003 existed) and is now corrected.

## IDN — Identity & Verification

Reconciled 2026-08-30: several boxes below were stale (unchecked despite being shipped in earlier PRs, well before external PIN/link verification started). Do not reimplement these.

- [x] **IDN-001** Define verification assurance levels and data types. — V0-V4 (`CapabilityRequest::ASSURANCE_LEVELS`, `SupportSession::assuranceLevel()`), used throughout the capability engine since the first PRs.
- [x] **IDN-002** Create verification challenge migration/repository. — `InstallSupportGatewayMigration::CHALLENGE_TABLE` + `DatabaseVerificationChallengeRepository`/`VerificationChallengeRepositoryInterface`. One shared table/model for both PIN and secure-link challenges (see ADR-005) — never two verification systems.
- [x] **IDN-003** Create support session migration/repository. — `InstallSupportGatewayMigration` + `DatabaseSupportSessionRepository`/`SupportSessionRepositoryInterface`, shipped with the earliest identity PRs.
- [x] **IDN-004** Prototype secure OJS-session ↔ Chatwoot conversation binding. — the `/bind` flow + `ChatwootConversationVerifier`, shipped with the earliest identity PRs.
- [x] **IDN-005** Write ADR selecting authenticated-session handshake. — `docs/v2/ADRS.md` ADR-005 "Adaptive verification".
- [x] **IDN-006** Implement logged-in OJS silent V2 identity path. — authenticated OJS session bootstraps a V2 support session with no PIN/email step (`SupportSessionService::bootstrapAuthenticated`).
- [x] **IDN-007** Implement OJS verification-code Mailable/template. — `SupportVerificationMailable` (extends PKP `Mailable`, sent via `Mail::send()` — real journal SMTP transport, not a bespoke mail stack). Deliberately does not use the full admin-configurable EmailTemplate framework yet (fixed, localizable-in-code content via `VerificationEmailContentBuilder` instead) — a scope decision, not an oversight; a future slice could register a real EmailTemplate row for Journal Setup > Emails customization.
- [x] **IDN-008** Implement verification request endpoint. — `POST /ojsSupportGateway/verificationRequest`.
- [x] **IDN-009** Implement verification confirm endpoint. — `POST /ojsSupportGateway/verificationConfirm` (PIN) + `GET /ojsSupportGateway/verify` (secure link, browser-facing).
- [x] **IDN-010** Implement secure verification link. — shares the same challenge engine as PIN (`VerificationChallengeService::confirmLinkToken()`); binds via the challenge's own server-side stored conversation tuple, since a browser opening the link cannot supply one.
- [x] **IDN-011** Store only challenge secret hash. — PIN: HMAC-SHA256 keyed by a per-journal pepper never stored in the challenge table (`chatwootVerificationPepper` plugin setting). Link: SHA-256 of a 256-bit token (already appropriate given the token's own entropy). See `VerificationSecretHasher`'s docblock for the reasoning.
- [x] **IDN-012** Implement single-use/expiry/revocation. — `VerificationChallenge::isConsumed()/isExpired()/isRevoked()`, enforced atomically in `DatabaseVerificationChallengeRepository::attemptConsume()` (single transaction, row-locked, consume guarded by a `WHERE consumed_at IS NULL` + affected-row-count check — a simultaneous replay can only produce one success). No public admin action to revoke a challenge exists yet (only supersession via resend); the state check itself is implemented and tested.
- [x] **IDN-013** Implement resend invalidation/cooldown. — `VerificationChallengeService::requestChallenge()`: a fresh valid request supersedes the prior unconsumed challenge for the same context+conversation+purpose; a cooldown (default 60s) silently throttles an immediate resend.
- [x] **IDN-014** Implement attempt lockout/rate limits. — per-challenge attempt lockout (default 5, configurable), plus rolling per-conversation and per-identity request limits (default 5 per hour each) — all enforced in `VerificationChallengeService`, all fail silently into the same generic response (never revealed to the caller).
- [x] **IDN-015** Implement anti-enumeration response/timing tests. — response-content anti-enumeration was already implemented/tested (nonexistent email, disabled account, throttling, and mail failure all produce the identical `{verificationRequested:true}` response). Timing is now also equalized: `ResponseTimingNormalizer::normalize()` pads every `ojs_request_verification` call up to a fixed 0.3s floor (never shortens an already-slow real response, which would itself become distinguishable), called unconditionally after the audit event and before the response is sent — regardless of which internal branch (not found/found/mail sent/exception) ran. Injectable sleeper keeps this real-behavior-tested without real wall-clock waits in CI.
- [x] **IDN-016** Implement support-session idle/absolute expiry. — `SupportSession::idleExpiresAt()`/`absoluteExpiresAt()`, enforced in `SupportApiRequestResolver`, shipped with the earliest identity PRs.
- [x] **IDN-017** Implement session revocation/cleanup task. — `revokeActiveUnboundForUser()` is wired into `SupportSessionService::bootstrapAuthenticated` (a fresh authenticated bootstrap revokes the user's own stale unbound sessions), and `purgeExpired()` on both the session and verification-challenge repositories is now actually swept: `PurgeExpiredSupportDataTask` extends the real `PKP\scheduledTask\ScheduledTask` and `ChatwootIntegrationV2Plugin implements PKP\plugins\interfaces\HasTaskScheduler`, registering the task daily via `PKP\scheduledTask\PKPScheduler::registerPluginSchedules()` — verified against a real local checkout of `pkp-lib` (the current 3.5 mechanism; the older `scheduledTasks.xml`/`getScheduledTaskPaths()` approach no longer exists in this codebase's target version).
- [x] **IDN-018** Implement conversation/context binding checks. — `SupportSession::matchesConversationBinding()` / `findByConversationBinding()`, shipped with the earliest identity PRs.
- [x] **IDN-019** Audit verification lifecycle. — `v2AuditVerificationEvent()` records request/PIN-confirm/link-confirm outcomes through the same persisted sink (AUD-001, `DatabaseSupportApiAuditLogger`) the Support API allow/deny decisions already use — reason codes only (`ChallengeAttemptOutcome::status()`, or `user_not_found`/`throttled`/`mail_not_configured`/`challenge_created`/`internal_error` for the request endpoint), never a PIN/token/email. Server-side only — never changes the caller-facing response, preserving anti-enumeration (IDN-015). `VerificationChallengeService` itself stays untouched (deliberately pure business rules, no audit/OJS coupling); the plugin endpoints that already call it own the audit call.

## REL — Relationship Resolver

- [x] **REL-001** Define normalized relationship enum/model. — `ResourceRelationship` (types set, not single role).
- [x] **REL-002** Resolve author/submitter relationship. — via workflow-stage access (`OjsSubmissionRelationshipEvidenceProvider`).
- [x] **REL-003** Resolve reviewer relationship per review assignment. — actual `Repo::reviewAssignment()` lookup, not the journal-level Reviewer role.
- [x] **REL-004** Resolve editor/sub-editor/assistant/manager/site-admin relationship.
- [x] **REL-005** Resolve reader/subscriber relationship if used. — not used: every existing relationship type (`ResourceRelationship`, `OjsSubmissionRelationshipEvidenceProvider`) is resolved *per submission* (workflow-stage access, review assignment) — a concept that does not apply to readers/subscribers, who have no relationship to any individual submission's workflow, only to already-published content. No capability, diagnostic, or serializer in the catalog reads a per-user reader/subscriber relationship anywhere; the only real subscription-related fact in this build is journal-level (`CorePublicationKnowledgeProvider`'s `Journal::PUBLISHING_MODE_*` knowledge fact), which already covers the one subscription-related question this build actually answers ("does this journal require a subscription"). Building an unused relationship type here would be exactly the kind of premature abstraction this build has otherwise avoided (see EVT-008/009's same verified-nothing-to-build finding).
- [x] **REL-006** Confirm context/resource ownership before relationship evaluation. — `SubmissionRelationshipResolver` fails closed on a cross-journal submission.
- [x] **REL-007** Multi-role resource-specific relationship tests. — `tests/v2/submission-verify.php` (author+reviewer same submission, plural, not collapsed).
- [x] **REL-008** Horizontal IDOR tests. — `tests/v2/submission-verify.php` (guessed submission ID, cross-journal submission, unrelated user all resolve to empty/no-error).

## POL — Capability & Privacy Policy

- [x] **POL-001** Define capability namespace. — dot-notation names in `CapabilityCatalog::DEFINITIONS` (`journal.read_public_info`, `submission.read_own_payment_status`, etc.).
- [x] **POL-002** Implement deny-by-default Policy Engine. — `CapabilityPolicyEngine::evaluate()`: providers only nominate, every nomination is re-checked against the catalog, an unknown capability fails closed.
- [x] **POL-003** Define public-support consumer plane. — the `public_support`/`support_escalation`/`account_support`/`submission_support`/`publication_support`/`payment_support`/`file_support` policy groups in `CapabilityCatalog`.
- [ ] **POL-004** Define staff consumer plane. — not started, deliberately: this build has stayed public-support-only throughout (the "no public staff mutations" guardrail); `CapabilityCatalog`'s docblock records staff/editorial capabilities as "intentionally absent from this first implementation."
- [x] **POL-005** Implement author support policy. — every author-exclusive capability already has its own independently-toggleable, real named journal policy in `CapabilityCatalog`: `submission.read_own_payment_status` -> `payment_support`, `submission.read_author_visible_files` -> `file_support` (both enforced live by `CapabilityPolicyEngine::evaluate()`'s `policyAllows()` check, proven independently switchable in `tests/v2/capability.php`/`tests/v2/payment-status.php`). Capabilities author and reviewer *share* (`submission.diagnose_own`, `submission.read_own_support_status`, etc.) correctly stay under `submission_support`/`publication_support` rather than an author-only name, since they are not author-exclusive. A single wrapping "author support policy" object would collapse these independently-configurable toggles into one on/off unit, which is false — the catalog's per-capability `policy` field already is the correct, real policy layer; no additional object was needed or built.
- [x] **POL-006** Implement reviewer support policy. — the `reviewer_support` policy group (`review.read_own_assignment`), gated on `REL-003`'s real `Repo::reviewAssignment()` lookup, never the journal-level Reviewer role.
- [ ] **POL-007** Implement staff read policy. — not started; same deliberate scope boundary as POL-004.
- [x] **POL-008** Implement field allowlist serializers. — every `classes/v2/Api/*Serializer.php` (`SubmissionListSerializer`, `PaymentStatusSerializer`, `PublicationStatusSerializer`, `RequiredActionsSerializer`, etc.) is an explicit field allowlist, never a raw-object passthrough.
- [x] **POL-009** Implement blind-review author serializer tests. — `tests/v2/blind-review-anonymity.php`: with two real reviewer assignments on one submission, the author's resolved relationship/serialized `SubmissionSupportSerializer` response is asserted to contain neither reviewer's user id nor any `reviewer_id`/`reviewerName`/`reviewerCount`/`reviewer` text.
- [x] **POL-010** Implement reviewer anonymity serializer tests. — same test file: reviewer A and reviewer B (different assignment statuses on the same submission) each resolve only their own relationship/status/required-action/serialized response — proven against the real `OjsSubmissionRelationshipEvidenceProvider`/`Ojs35CompatibilityAdapter::getReviewAssignmentStatuses()`/`RequiredActionMapper`/`SubmissionSupportSerializer` end-to-end, not isolated units. The underlying guarantee is structural: those lookups only ever call `filterByReviewerIds([$userId])` — "does this user have an assignment," never "list every reviewer" — so there was no enumeration path to redact in the first place.
- [x] **POL-011** Replace v1 global reviewer masking behavior. — closed via CWO-016 above: `ChatwootIntegrationBasePlugin::addChatwootWidget()`'s `is_masked` logic now derives from a real resource-scoped relationship check, not merely the journal-wide Reviewer role. Real-browser acceptance evidence remains POL-015 (open).
- [x] **POL-012** Implement `get_available_actions`. — `AvailableActionMapper::map()`, converting a `CapabilityDecision` into stable support-facing action names (`RequiredActionsSerializer` consumes it).
- [x] **POL-013** Audit allow/deny reason codes. — `CapabilityDecision`'s denial reasons, filtered through `AvailableActionMapper::SAFE_DENIAL_REASONS` before ever reaching a support-facing response (internal plumbing states excluded).

## API — REST Support API

- [x] **API-001** Implement API route/PageHandler strategy per supported OJS versions. — `LoadHandler` + `SupportGatewayPageHandler`, same pattern as `/bind`; status/identity/actions operations.
- [x] **API-002** Implement service authentication. — `ServiceTokenAuthenticator`, Bearer token, comma-separated for rotation, constant-time comparison.
- [x] **API-003** Require HTTPS/config check for production. — `SupportApiRequestResolver::transportSecure()`; accepts direct HTTPS or `X-Forwarded-Proto: https` from a fronting reverse proxy. No trusted-proxy allowlist yet (see its docblock).
- [x] **API-004** Implement correlation/request IDs. — `CorrelationId`; echoes a caller-supplied `X-Correlation-Id` when it matches a safe pattern, otherwise generates one.
- [x] **API-005** Implement common response/error envelope. — `SupportApiResponse` (`{ok,data,meta}` / `{ok,error,meta}`), used by status/identity/actions. `/bind` intentionally keeps PKP `JSONMessage` (browser handshake, separate transport).
- [x] **API-006** Implement request validation/unknown-field rejection. — `SupportApiFieldAllowlist::firstUnknownField()`, a real per-endpoint allowlist derived from exactly the fields each of the 14 `resolveSupportApiRequest()`-gated endpoints actually reads, wired into `resolveSupportApiRequest()` itself right after `SupportApiRequestResolver::resolve()` succeeds — checked only once a request is already authenticated, so an unauthenticated caller can never use field names as a probing oracle. The two non-enveloped transports (`bindSupportSessionRequest`'s PKP `JSONMessage` handshake, `verifyLinkRequest`'s browser link click — both already noted as a separate transport under API-005) are out of this scope, same boundary already established there.
- [x] **API-007** Trusted proxy + rate-limit closure. `SupportApiRequestResolver::transportSecure()` no longer trusts a caller-supplied `X-Forwarded-Proto` header unconditionally — it now gates that trust on OJS core's own real, existing `config.inc.php` flag (`[general] trust_x_forwarded_for`, the same one `PKPRequest::getRemoteAddr()` already uses to decide whether `X-Forwarded-For` may be trusted), rather than inventing a second, unchecked plugin-specific proxy-trust setting. Proven behaviorally (not just by source string match) in `tests/v2/api-trusted-proxy-transport.php`: a caller forging `X-Forwarded-Proto: https` over plain HTTP is rejected when no trusted proxy is declared, is accepted once it is, a declared-trusted proxy reporting plain `http` is still rejected, and the OJS-core backward-compatible default (`true` when the key is entirely absent) is preserved. The best-effort per-worker `RateLimiter` (APCu, fails open, defense-in-depth — never the primary security boundary, which is the bearer service token plus exact bound-conversation match) is kept as-is rather than replaced with a mandatory shared store: `docs/v2/SECURITY_PRIVACY.md` §4.4 now documents the actual deployment model — the primary rate-limit ceiling belongs at the fronting reverse-proxy/load-balancer layer (nginx `limit_req`, Cloudflare rate limiting, etc.), which holds across every worker/machine without requiring Redis; the plugin's APCu limiter is the second, independent layer; upgrading to a shared store is a documented future step, contingent on real abuse evidence, not attempted speculatively.
- [x] **API-008** `ojs_get_support_identity`. — `POST /ojsSupportGateway/identity`, sanitized via `SupportIdentitySerializer` (no email, no raw relationship evidence, no raw OJS object).
- [x] **API-009** `ojs_list_my_submissions`. — `POST /ojsSupportGateway/submissions`, gated on `submission.list_own`, candidates from the OJS-native submission collector filtered through `SubmissionRelationshipResolver`. No milestone/date field yet.
- [x] **API-010** `ojs_get_submission_support`. — `POST /ojsSupportGateway/submissionSupport`, establishes its own request-time V3 the same way `submissionVerify` does, gated on `submission.read_own_support_status`. Returns normalized support state, one safe explanatory sentence (`SupportStateMapper::explain()`), title, relationships, and capability-derived actions. Required actions, publication detail, and milestone dates are separate not-yet-built endpoints (API-011/API-012/API-013) — this one does not anticipate their shape.
- [x] **API-011** `ojs_get_required_actions`. — `POST /ojsSupportGateway/requiredActions`, establishes its own request-time V3 the same way `submissionVerify`/`submissionSupport` do, gated on `submission.read_own_required_actions`. New `RequiredActionMapper` only reports an action directly provable from existing evidence: author `draft`→`complete_submission`, `revision_requested`→`submit_revisions`; reviewer statuses via PKP's own computed `ReviewAssignment::getStatus()` (verified against `pkp-lib` stable-3_5_0) mapped to `respond_to_review_invitation`/`submit_review`, most-urgent-wins across multiple review rounds. Empty list is a correct answer for every other state, not a placeholder.
- [x] **API-012** `ojs_get_payment_status`. — `POST /ojsSupportGateway/paymentStatus`, gated on `submission.read_own_payment_status` (V3 + author only; verified against `pkp-lib`/`ojs` `stable-3_5_0` `OJSPaymentManager`/`OJSCompletedPaymentDAO`, never re-derives their logic). Public fee facts (`feeEnabled`/`amount`/`currency`) are always returned regardless of verification — they're journal-level, not submission-specific. `payment_status` feature flag is derived live from `OJSPaymentManager::isConfigured() + publicationEnabled()`. The submission-specific `status` (paid/unpaid/not_applicable) additionally requires the `payment_support` journal policy, which defaults to `false` with no admin toggle built yet — intentionally unreachable in production until PRV/settings work exists to opt a journal in. `waived` is not implemented for the native OJS producer: no genuine evidence of a fee-waiver concept was found in OJS core. Since PRV-001/PRV-005, the endpoint additionally returns an additive `obligations` array (empty unless the Airix Submission Fee sibling plugin is installed/enabled/compatible) via `SupportProviderRegistry`; when a provider reports an obligation it — not the native publication fee — becomes the authoritative `status`/`amount`/`currency` for that submission (see `AIRIX360_INTEGRATIONS.md` §5.8 on producer vs. collector). This is the one Airix payment adapter built so far (APS-*); waiver/gateway/orchestrator adapters (AWA/APM/AGW) remain unbuilt.
- [x] **API-013** `ojs_get_publication_status`. — `POST /ojsSupportGateway/publicationStatus`, establishes its own request-time V3 the same way the other submission-scoped endpoints do, gated on `submission.read_own_publication_status`. `doi`/`publicUrl`/`issue` are only populated when the normalized support state is exactly `published` or `scheduled_for_publication`; `publicUrl` further restricted to `published` only (a scheduled article isn't yet publicly visible). Issue metadata (volume/number/year) is only surfaced when the linked `Issue` itself reports `getPublished() === true`, as a fail-safe against ever leaking an unpublished issue's data through a published article. Every other state returns `status: 'not_yet_published'` with no other fields.
- [x] **API-014** `ojs_diagnose_account`. — `POST /ojsSupportGateway/accountDiagnostics`, gated on new capability `account.diagnose_own` (V2, no resource relationship — this is the caller's own account). New `classes/v2/Diagnostics/DiagnosticResult.php` (shared status/code/summary/evidenceCodes/nextActions/retryable contract, reused by future `ojs_diagnose_submission`) and `AccountDiagnosticEngine` covering scopes `account_access`/`login`/`profile` as `confirmed` (from `User::getDisabled()`/`getDateValidated()`, verified against `pkp-lib` stable-3_5_0) and `password_reset` always as `unknown` (no OJS evidence of email delivery exists). Never accepts a caller-supplied email/username — diagnoses only the verified caller's own account, never an arbitrary lookup.
- [x] **API-015** `ojs_diagnose_submission`. — `POST /ojsSupportGateway/submissionDiagnostics`, gated on new capability `submission.diagnose_own` (V3, author/reviewer). New `SubmissionDiagnosticEngine` covers scopes `submission_access`/`submission_progress`/`required_action`/`review_access`/`publication`/`payment` — every scope is a thin wrapper over the existing `SubmissionRelationshipResolver`/`SupportStateMapper`/`RequiredActionMapper`/publication+payment field services, not a second workflow interpreter. The `payment` scope independently re-evaluates `submission.read_own_payment_status` exactly like the dedicated payment endpoint, so it can never reveal more than that endpoint would (still gated off by the `payment_support` policy default).
- [x] **API-016** `ojs_escalate_support`. — `POST /ojsSupportGateway/escalate`, gated on the existing `support.escalate` capability (deliberately V0/unauthenticated — a handoff must remain available even when verification itself is failing). New `HandoffSummaryFormatter` (HOF-001..006) composes a safe DTO from `SupportIdentitySerializer` (verification method/expiry) plus, only when independently re-checked against each fact's own dedicated capability (`submission.read_own_support_status`/`read_own_required_actions`/`read_own_publication_status`/`read_own_payment_status`), resource relationship/support state/required actions/publication/payment facts — never more than the verified caller could already read via those dedicated endpoints. Posts a Chatwoot private note via the existing (v1) `ChatwootApiService::createConversationNote()`, reused rather than rebuilt; posting is best-effort and never fails the whole request. `EscalationIdempotencyGuard` (HOF-007) is APCu-backed, same fail-open/per-worker character as `RateLimiter` — a durable idempotency ledger is the upgrade path, not built here.
- [x] **API-017** Generate OpenAPI/machine-readable schema. — new `docs/v2/openapi.json` (OpenAPI 3.0.3, JSON rather than YAML since no YAML PHP extension is available to parse it in CI/tests). Covers all 14 real, implemented `SupportGatewayPageHandler` operations (`status`/`identity`/`actions`/`submissionVerify`/`submissions`/`submissionSupport`/`requiredActions`/`publicationStatus`/`paymentStatus`/`accountDiagnostics`/`submissionDiagnostics`/`escalate`/`verificationRequest`/`verificationConfirm`) — every request/response shape transcribed directly from the real serializer/inline-array code, never fabricated. Shared shapes (`SuccessEnvelope`/`ErrorEnvelope`/`Meta` matching the real `SupportApiResponse` envelope, `ConversationTuple`, `ResourceScopedUnverified`'s anti-enumeration shape, `SubmissionVerificationData` as the common base every resource-scoped `verified` shape extends via `allOf`) are defined once and referenced, not duplicated per endpoint. The MCP (JSON-RPC) surface is explicitly out of scope here — it isn't REST-shaped, and is already documented in ADR-023/API_MCP_SPEC.md.
- [x] **API-018** Contract-test all public responses. — new `tests/v2/openapi-contract.php`: (1) for `/actions` and `/requiredActions` (one hand-built REST-shape fixture, one built through the real shared `RequiredActionsSerializer::verified()` call site), asserts the schema's declared properties and the real response's actual keys match in both directions — an undeclared key the code emits, or a declared key the code never emits, both fail; (2) for every one of the 14 paths in `openapi.json`, asserts a real `{operation}()` method exists on `SupportGatewayPageHandler` — the schema can never document an endpoint that doesn't exist. Deliberately does not pull in a general-purpose JSON Schema validator dependency for this scope.
- [x] **API-019** Keep Captain-provisioned custom tools <=12. — `CanonicalToolCatalog::all()` defines exactly 12, enforced by a test assertion (`count($tools) <= 12`), well under Chatwoot's own account-wide cap of 15 (`Captain::CustomTool::MAX_PER_ACCOUNT`).

## STA — Support State Engine

- [x] **STA-001** Define normalized support states. — `SupportStateMapper` (`classes/v2/State/`) now covers draft/submitted/review_in_progress/revision_requested/copyediting_in_progress/production_in_progress/published/declined/scheduled_for_publication/unknown. Correction to an earlier note: draft did *not* need a separate candidate-discovery path — OJS 3.5 creates the author's StageAssignment immediately at submission creation (verified against `pkp-lib` stable-3_5_0 `PKPSubmissionController::add()`), so the existing `assignedTo()`-based discovery already reaches drafts; only `submissionProgress` needed reading. Still missing: revision_received (needs revision-file evidence).
- [x] **STA-002** Map submission stages/statuses for OJS 3.5 target. — `status`/`stageId` mapped as before; `revision_requested` now additionally derived from the current review round's live `status` column (`ReviewRoundDAO::getLastReviewRoundBySubmissionId`, verified against `pkp-lib` stable-3_5_0), read-only, never recomputed independently.
- [ ] **STA-003** Map OJS 3.6 target after compatibility verification.
- [x] **STA-004** Determine safe reviewer-progress facts without identities. — `Ojs35CompatibilityAdapter::getCurrentReviewRoundStatus()`: the review round's own aggregate `status` column (`ReviewRoundDAO::getLastReviewRoundBySubmissionId()`, verified against `pkp-lib` stable-3_5_0, maintained live by pkp-lib itself) — a round-level signal, never tied to any specific reviewer's identity or individual progress. This is the only safe aggregate fact pkp-lib's real review-round model exposes; there is no "X of Y reviews completed" count to read beyond this status enum.
- [x] **STA-005** Determine author-action-required rules. — `RequiredActionMapper::forAuthor()` covers the two provable cases (draft→complete_submission, revision_requested→submit_revisions); every other state correctly returns no action rather than a guess.
- [x] **STA-006** Determine revision status/deadline rules. — status half already covered by STA-002/STA-004 (round-level `revision_requested` detection). The deadline half has nothing to build: verified against a real local pkp-lib checkout that `schemas/decision.json` (the real editorial-decision data model) has no due-date/deadline field at all — only `dateDecided`. `dateDue`/`dateResponseDue` exist only on `ReviewAssignment` (a reviewer's own due date to submit their review), never on the decision that requests author revisions. Any deadline communicated to an author for revisions is prose in the decision email, not a queryable fact — consistent with EVT-008/009's same verified-nothing-to-build finding.
- [x] **STA-007** Determine copyediting/production/publication rules. — already fully covered by `SupportStateMapper::map()`'s existing `stageId`/`status` branches (`copyediting_in_progress`/`production_in_progress`/`published`/`scheduled_for_publication`), verified against a real local pkp-lib checkout that there is no finer per-stage completion signal to add: `classes/submission/maps/Schema.php::getPropertyStages()` only ever exposes `isActiveStage` (`== stageId`), `editorAssigned`, and an editing-stage-only `uploadedFilesCount` — no `stages[$id]['status']`/completed flag exists anywhere in pkp-lib's own stage model. A stage transition (`stageId` moving from EDITING to PRODUCTION) *is* what "copyediting complete" means in OJS's own data model; there is no separate fact to read. Publication-specific nuance (embargo/issue-publication-gating/DOI/publicUrl) already lives one layer up in `ojs_get_publication_status` (API-013), which is the correct place for it — `SupportStateMapper` only needs the coarse state.
- [x] **STA-008** Return confidence/evidence codes. — `SupportStateMapper::confidence()`/`evidenceCode()`, derived from the exact same branches as `map()` via a shared private resolver (never a second, potentially-drifting judgment call). Every non-`unknown` state is `DiagnosticResult::STATUS_CONFIRMED` — this mapper only ever reads directly authoritative OJS fields, never infers circumstantially, so there is no `LIKELY` tier here. Wired additively into `ojs_get_submission_support`'s response as `stateConfidence` (omitted entirely, not null, when not computed) — `map()`'s own signature/behavior is unchanged, so every other caller is unaffected.
- [x] **STA-009** Unknown-state fallback tests. — `tests/v2/submission-list.php` covers unrecognized status, unrecognized stageId, and missing status/stageId all falling back to `unknown` rather than a guess.

## KNO — Journal Knowledge Compiler

Note: this is the foundation slice only, per the compiler-freeze directive — a
KnowledgeProvider must never depend on a SupportSession, Chatwoot conversation,
OJS user, submission relationship, or V2/V3 capability, and only the exact
classification `public` (not `private`/`unsupported`, and never an
unrecognized value) ever reaches a generated page. Categories are built
incrementally (about/submissions/review/policies first); fees/publication/
accounts/official-pages/sitemap/Captain sync are deliberately deferred to
later PRs, not attempted in one pass.

`PaymentSupportProviderInterface` (Provider Registry, PRV/AXP/APS above) and
`KnowledgeProviderInterface` are different trust contracts and must not be
merged just because both are "providers": a submission's specific payment
*obligation* (paid/unpaid/waived/refund_review/refunded) is private live
state and must never become a KnowledgeFact; a journal's configured fee
*policy* ("this journal charges a $50 submission fee") could become public
knowledge, but only through a provider explicitly implementing
`KnowledgeProviderInterface` — never inferred by a Knowledge provider
inspecting `AirixSubmissionFeeProvider`'s obligations.

- [x] **KNO-001** Define `KnowledgeProvider` contract. — `KnowledgeProviderInterface` (`classes/v2/Contracts/`).
- [x] **KNO-002** Define classification/provenance model. — `KnowledgeFact` (key/value/classification/source/sourceReference/locale/updatedAt/providerId) + `KnowledgeClassification` (`public`/`private`/`unsupported` only; the constructor rejects anything else).
- [x] **KNO-003** Core journal/contact provider. — `CoreJournalKnowledgeProvider`: name, description, about, publisher, contact name/email, ISSN (online/print), languages, public journal URL — every accessor verified against a real local checkout of `pkp-lib`/`ojs` `stable-3_5_0` (`schemas/context.json`, `classes/context/Context.php`).
- [x] **KNO-004** Submission guidelines/checklist provider. — folded into `CoreJournalKnowledgeProvider` (`submission.authorGuidelines`, `submission.checklist`) rather than a separate class; one core provider for now, split out only if a real second source needs its own adapter.
- [x] **KNO-005** Sections/languages provider. — folded into `CoreJournalKnowledgeProvider` (`submission.sections` via `Repo::section()->getSectionList()`, `journal.languages` via `getSupportedLocales()`).
- [x] **KNO-006** Review policy provider. — folded into `CoreJournalKnowledgeProvider` (`review.model`, deterministically derived from `ReviewAssignment::SUBMISSION_REVIEW_METHOD_*` — never an invented sentence like "reviews take 4-6 weeks"; `review.guidelines` from the journal's own configured text).
- [x] **KNO-007** Publication/open-access/licence provider. — `policy.copyright`/`policy.licenseTerms`/`policy.licenseUrl` (KNO-003) plus new `CorePublicationKnowledgeProvider`: `publication.accessModel` (deterministic sentence from `Journal::PUBLISHING_MODE_{OPEN,SUBSCRIPTION,NONE}`, verified against `ojs` `stable-3_5_0`; a configured `delayedOpenAccessDuration` is stated verbatim, never an invented timeline), `policy.openAccessPolicy` (the journal's own configured text), `publication.doiAssigned` (from `enableDois`), `publication.currentIssueUrl`/`publication.archiveUrl` (the real `issue/current`/`issue/archive` page routes OJS core itself uses in `NotificationManager`/`OJSPaymentManager`/`SitemapHandler`). DOI prefix/suffix pattern, archival-preservation participation (CLOCKSS/LOCKSS), and publication frequency are not implemented — deferred, not guessed.
- [x] **KNO-008** APC/payment public policy provider. — `CorePaymentKnowledgeProvider`: native OJS publication fee (`fee.publicationEnabled`/`Amount`/`Currency`, via the same verified `OJSPaymentManager` path `ojs_get_payment_status` uses) and Airix Submission Fee policy (`fee.submissionEnabled`/`Amount`/`Currency`, via the new `Ojs35CompatibilityAdapter::getAirixSubmissionFeePolicy()` — policy-only, calls only `PaymentHelper::feeEnabled()/amount()/currency()`, never `hasPaid()`/`waiverDiscount()`/`needsRefundReview()`). Distinct key prefixes (`publication`/`submission`) so the two producers can never collide or silently overwrite each other. Waiver policy text and payment instructions are not implemented — no verified public-facing accessor was found for either.
- [x] **KNO-009** DOI/public identifier provider. — `CorePublicationKnowledgeProvider::addDoiPrefix()`: the journal's own `doiPrefix` setting (`Context::SETTING_DOI_PREFIX`, verified against a real local `pkp-lib` checkout — `classes/doi/Repository.php` reads it the same way to build a complete DOI). Public by nature (registered with the DOI agency), so surfaced verbatim. Only ever emitted alongside `publication.doiAssigned` (i.e. only when DOIs are actually enabled for the journal) — a stale prefix setting on a journal with DOIs disabled is never surfaced. The DOI prefix/suffix *pattern* (i.e. how article-level suffixes are generated) remains unimplemented — no verified public-facing accessor was found for that.
- [x] **KNO-010** Official page/navigation provider. — `OfficialPageKnowledgeProvider`, narrowly scoped to OJS core's own Static Pages plugin (verified against a real local checkout of `pkp/staticPages` @ `0a84bbe738b3356ac57fe99c66f2792f0d7016bb`) via new `Ojs35CompatibilityAdapter::getOfficialPublicPages()` — never a crawl of arbitrary journal-domain URLs. Each journal-manager-authored static page becomes one `officialPage.<path>` fact, title+content sanitized. Ranked lowest of the structured sources in `KnowledgeSourcePrecedence` (tier 3), so a stale static page can never override current structured configuration.
- [ ] **KNO-011** Approved FAQ/support knowledge provider. — verified against real `chatwoot/chatwoot` `develop` source (`config/routes.rb`'s `namespace :captain do resources :assistant_responses ... resources :faq_suggestions ... end`, `enterprise/app/models/captain/assistant_response.rb`, `enterprise/app/services/captain/faq_suggestion_approval_service.rb`): a real, filterable `GET .../captain/assistant_responses?assistant_id=X` endpoint exists, and every `Captain::AssistantResponse` row *is* an approved FAQ by construction (`enum status: { approved: 1 }` — the model has no other status; the open/approved/dismissed review lifecycle lives entirely on the separate `Captain::FaqSuggestion`, which `FaqSuggestionApprovalService#perform` converts into an `AssistantResponse` on approval). `KnowledgeSourcePrecedence::SOURCE_FAQ` (tier 4, lowest) already anticipates this provider. Not yet built: every existing `KnowledgeProviderInterface` implementer is constructed once in the version-generic `SupportGatewayKernel::forOjsVersion()`, before any per-journal Chatwoot base URL/token/assistant ID is known, and `collect()` is meant to stay a fast, safe path for an anonymous public visitor (unlike `CaptainProvisioningHealthService`, which is deliberately a local-only read for exactly this reason) — a live Chatwoot API call on every anonymous `/support-knowledge/` page load would be a real reliability/latency risk with no caching layer to fall back on. This needs a periodic local sync (mirroring `CaptainDocumentProvisioner`'s existing OJS-to-Chatwoot sync pattern, but in the opposite direction) into a local cache table before a provider can safely read it — a genuinely larger, separate slice, not attempted here to avoid rushing a public-facing path.
- [x] **KNO-012** Fingerprinting/staleness model. — `KnowledgeFingerprint::compute()`: deterministic, order-independent, sensitive only to normalized `locale`+`key`+`value` (never rendered HTML, never provenance metadata).
- [x] **KNO-013** Generate `/support-knowledge/` root. — `support-knowledge` page (`SupportKnowledgePageHandler::index`), links every generated category page.
- [x] **KNO-014** Generate category pages. — `about`/`submissions`/`review`/`fees`/`publication`/`pages`/`accounts`/`policies` — the full first-slice category set.
- [x] **KNO-014a** Precedence/conflict handling (not separately numbered in the original backlog; added once a second structured/semi-structured source existed). `KnowledgeSourcePrecedence` ranks sources into the four tiers from the freeze directive (structured OJS config > verified structured third-party provider > official OJS-managed page > approved FAQ; an unrecognized source ranks last, never silently wins). `KnowledgeCompiler` deduplicates same-`(locale,key)` collisions by rank and records every loser as a `KnowledgeConflict` on the compilation — never rendered, exposed only as safe metadata via `KnowledgeHealthService` (KNO-020).
- [x] **KNO-015** Generate sitemap. — `/support-knowledge/sitemap` (see the URL-suffix note in `KNOWLEDGE_DIAGNOSTICS.md` §4 — PKP page/operation routing cannot dispatch a literal `sitemap.xml` segment to a handler method), `Content-Type: application/xml`, XML-escaped, drawing its route list from the same `KnowledgeRouteCatalog` the root navigation uses.
- [x] **KNO-016** HTML sanitization/escaping. — `KnowledgeSanitizer`: strips `script`/`style`/`iframe`/`object`/`embed`/`form`/`applet`/`link`/`meta`/`base`/`noscript`, event-handler attributes, and `javascript:`/`vbscript:`/`data:` URLs; permits a small safe presentation tag subset. Regex-based by design, not a new HTML-parser dependency (PR #19 lesson).
- [x] **KNO-017** Private-data exclusion tests. — `tests/v2/knowledge-compiler.php`: multi-journal isolation, private/unsupported facts never render, provider-exception isolation, no `SupportSession`/conversation ID/Chatwoot token/payment-obligation-state string anywhere in `classes/v2/Knowledge/`.
- [x] **KNO-018** Multi-locale support. — `KnowledgeCompiler::resolveLocale()`: requested locale -> journal-supported locale -> journal primary locale -> `en`, reusing `Context::getSupportedLocales()`/`getPrimaryLocale()` rather than re-deriving OJS's own locale rules; fingerprint is per resolved locale.
- [x] **KNO-019** Captain document sync trigger/manual fallback. — partial: `CaptainSyncScheduledTask` now drives `provisionCaptainKnowledgeDocument()`/`provisionCaptainCustomTools()`/`provisionCaptainScenarios()` daily for every enabled journal, registered alongside `PurgeExpiredSupportDataTask` via the same `HasTaskScheduler`/`PKPScheduler` mechanism (IDN-017). Tools are provisioned before scenarios per journal since a scenario instruction can only reference an already-assigned tool slug. A manual on-demand trigger (an admin settings action, Phase 13) is still a separate, unbuilt path — this closes the periodic/automatic half of the item, not the manual-fallback half.
- [ ] **KNO-020** Knowledge health UI. — partial: the service/model half is done: `KnowledgeHealthService`/`KnowledgeHealthReport` (deterministic `healthy`/`degraded`/`empty`/`failed` state, per-provider health, excluded-fact counts, safe conflict metadata, generated-routes list). The overall Knowledge health state is now surfaced in the admin settings form's Support Gateway Health section (ADM-002). A dedicated per-provider/conflict detail view is still open.

### Also added this phase (accounts category, KNO-006 scope)

- `accounts.registrationAvailable`/`registrationUrl` (only when available)/`loginUrl`/`passwordResetUrl`/`orcidEnabled` — `AccountsKnowledgeProvider`, verified against `pkp-lib` `stable-3_5_0` (`RegistrationHandler::validate()`'s exact `disableUserReg` gate, `\PKP\orcid\OrcidManager::isEnabled()`, and the real `user/register`/`login`/`login/lostPassword` routes OJS core's own frontend templates use). Journal-specific approved FAQs (the rest of the original "Accounts/support" domain) remain unbuilt.

## PAY — Payment & Publication

- [x] **PAY-001** Read public publication fee/currency through OJS adapter. — `Ojs35CompatibilityAdapter::getPaymentFeeInfo()`.
- [x] **PAY-002** Resolve completed publication payment for authorized submission. — `Ojs35CompatibilityAdapter::hasPaidPublicationFee()`.
- [x] **PAY-003** Normalize `not_applicable|unpaid|paid|waived|unknown`. — `PaymentObligationStatus` (also `PARTIALLY_WAIVED`/`REFUND_REVIEW`/`REFUNDED`, a superset of the originally-scoped set).
- [x] **PAY-004** Verify unauthorized submission payment query is denied. — `tests/v2/payment-status.php`'s "capability denied by default (payment_support policy is off)" case, exercised through the real `CapabilityPolicyEngine`.
- [x] **PAY-005** Ensure payment provider failure returns unknown, not unpaid. — `SupportProviderRegistry::resolveObligations()`'s `ObligationResolution::hasFailures()` branch; `supportPaymentStatusRequest()` reports `PaymentObligationStatus::UNKNOWN` rather than silently falling back to a different producer's state (verified by both `tests/v2/provider-registry.php` and a plugin-source check).
- [x] **PAY-006** Publication status provider. — `PublicationStatusSerializer` + `Ojs35CompatibilityAdapter::getPublicationFields()`.
- [x] **PAY-007** Issue assignment/public URL provider. — `Ojs35CompatibilityAdapter::getIssueInfo()`/`getPublicSubmissionUrl()`.
- [x] **PAY-008** DOI safe status provider. — `getPublicationFields()`'s `doi` field, exposed through `PublicationStatusSerializer`.
- [x] **PAY-009** Keep public plane payment writes disabled. — no payment write/mutation endpoint exists anywhere in the plugin; confirmed by absence rather than an explicit block (there is nothing to disable — consistent with the "no public staff mutations" guardrail applied throughout this build).

## DIA — Diagnostics

- [x] **DIA-001** Define diagnostic result schema. — `DiagnosticResult` (`confirmed`/`likely`/`unknown`/`needs_human`, privacy-safe machine-readable evidence codes only, never a raw DAO row/exception message/internal config value).
- [x] **DIA-002** Account/login diagnostic. — `AccountDiagnosticEngine::SCOPE_ACCOUNT_ACCESS`/`SCOPE_LOGIN`.
- [x] **DIA-003** Registration/reset-path diagnostic. — `AccountDiagnosticEngine::SCOPE_PASSWORD_RESET` (deliberately always `unknown` — no OJS evidence about email delivery/reset-link validity exists to check, so this scope's whole job is refusing to guess) / `SCOPE_PROFILE` for email validation.
- [x] **DIA-004** Submission access diagnostic. — `SubmissionDiagnosticEngine::SCOPE_SUBMISSION_ACCESS`.
- [x] **DIA-005** Submission workflow/progress diagnostic. — `SubmissionDiagnosticEngine::SCOPE_SUBMISSION_PROGRESS`.
- [x] **DIA-006** Required metadata/file diagnostic where deterministic. — new `SubmissionDiagnosticEngine::SCOPE_REQUIRED_FILES`/`diagnoseRequiredFiles()`, backed by `Ojs35CompatibilityAdapter::getMissingRequiredSubmissionFileGenreNames()`: compares the journal's Airix `RequiredSubmissionFilesPlugin`-configured required genres against the submission's real uploaded files (`Repo::submissionFile()`). Deterministic by construction — a genre either has a matching file or it does not. "Nothing required" and "everything uploaded" both report the same `REQUIRED_FILES_COMPLETE`, since both are factually true regardless of which applies. Wired into `supportSubmissionDiagnosticsRequest()`'s real scope dispatch, not just built-and-unused.
- [x] **DIA-007** Upload PHP/OJS limit diagnostic. — new `Ojs35CompatibilityAdapter::getUploadLimits()` reads the same `upload_max_filesize`/`post_max_size` php.ini values pkp-lib itself derives its `UPLOAD_MAX_FILESIZE` constant from (there is no separate OJS-level setting). `SubmissionDiagnosticEngine::SCOPE_UPLOAD_LIMIT`/`diagnoseUploadLimit()` flags the one universally well-known, deterministic misconfiguration (`post_max_size` below `upload_max_filesize`) — never a guessed "too small" threshold, since none is universally correct. System-wide, not submission-specific; wired into the same `supportSubmissionDiagnosticsRequest()` scope dispatch as the others.
- [x] **DIA-008** Review access diagnostic. — `SubmissionDiagnosticEngine::SCOPE_REVIEW_ACCESS`.
- [x] **DIA-009** Payment diagnostic. — `SubmissionDiagnosticEngine::SCOPE_PAYMENT`.
- [x] **DIA-010** Publication/DOI diagnostic. — `SubmissionDiagnosticEngine::SCOPE_PUBLICATION`.
- [x] **DIA-011** Mail configuration/send-path diagnostic. — `AccountDiagnosticEngine::SCOPE_MAIL_CONFIGURATION`, backed by `Ojs35CompatibilityAdapter::getMailTransportConfiguration()`, which mirrors pkp-lib's real transport-selection logic exactly (`classes/core/PKPContainer.php::getDefaultMailer()`, verified against a real local pkp-lib checkout): sandbox mode forces the `log` driver regardless of the configured `[email] default`; `smtp` additionally requires a non-blank `smtp_server`. A config-shape check, distinct from `SCOPE_PASSWORD_RESET`'s still-open delivery-evidence gap (this never claims a specific email was sent or received).
- [ ] **DIA-012** Public vs staff diagnostic serializers. — not done, deliberately: only `DiagnosticResultSerializer` (public) exists — same scope boundary as POL-004/007 (no staff consumer plane built yet).
- [x] **DIA-013** Unknown/needs-human behavior tests. — extensively covered for `unknown` across both `tests/v2/account-diagnostics.php` and `tests/v2/submission-diagnostics.php` (missing evidence, ambiguous null fields, unrecognized scope, denied capability never revealing specific state). No engine currently constructs `needs_human` — the status exists in the schema (`DiagnosticResult::needsHuman()`) but no scope has a real trigger for it yet, so that half of the status enum is untested by necessity, not by omission.

## EVT — Event Bridge

- [x] **EVT-001** Define normalized `SupportEvent` model. — `SupportEvent` (immutable DTO: type/contextId/resource/idempotencyKey/occurredAt/attributes, never a delivery mode — that's EVT-010's job) + `SupportEventType` (the 7 real v1-derived event kinds, dot-notation named consistent with `CapabilityCatalog`). Not yet wired to any real OJS hook — that begins at EVT-003.
- [x] **EVT-002** Stable idempotency keys. — `SupportEvent::create()`'s `$naturalKey` param + `deriveIdempotencyKey()`: a deterministic hash of (type, contextId, resourceType, resourceId, naturalKey) — never a random value, never dependent on `occurredAt`, so a genuine retry of the same real occurrence always collides while a different decision/type/resource/journal never does. `naturalKey` sourcing per real event (e.g. a real OJS decision's own id for `submission.decision_recorded`) is left to each EVT-003+ event adapter, since only that adapter knows the real distinguishing detail for its event kind.
- [x] **EVT-003** Migrate v1 submission-created event. — `SubmissionCreatedEventAdapter::fromSubmission()` converts a real submission into a normalized `submission.created` `SupportEvent` (excluding author identity, a delivery-target concern not an event fact), now wired into `ChatwootIntegrationV2Plugin::handleSubmissionCreated()` — v1's real handler runs first and unconditionally, its return value is preserved exactly; the v2 conversion+enqueue is wrapped in its own try/catch so it can never affect the real hook's outcome. Enqueue-only: nothing yet reads the queue and delivers to Chatwoot (EVT-011).
- [x] **EVT-004** Migrate v1 decision event. — `DecisionRecordedEventAdapter::fromDecision()` converts a real editorial decision into a normalized `SupportEvent`, mirroring `handleEditorDecision()`'s `mapDecisionEventKey()` — a decision code maps to the specific `submission.revision_requested`/`accepted`/`rejected` type where one applies (see EVT-006), falling back to the generic `submission.decision_recorded` otherwise (excluding author identity, same as EVT-003). Idempotency key is keyed on the decision's own id, since a submission receives many decisions over its lifetime. Now wired into `handleEditorDecision()` the same way as EVT-003, enqueue-only.
- [x] **EVT-005** Migrate v1 status/publication event. — `SubmissionStatusChangedEventAdapter` (mirrors `handleSubmissionStatusUpdated()`) and `PublicationStatusEventAdapter` (mirrors `handlePublicationPublished()`, keyed on the publication's own id) both convert real OJS state into normalized events, now wired into their real hooks the same way as EVT-003, enqueue-only.
- [x] **EVT-006** Add revision event adapters where stable. — v1 has no separate revision hook; `DecisionRecordedEventAdapter` (EVT-004) now maps `PENDING_REVISIONS`/`RESUBMIT`/`RECOMMEND_PENDING_REVISIONS`/`RECOMMEND_RESUBMIT` decision codes to `submission.revision_requested`, the exact same real source `handleEditorDecision()`'s `mapDecisionEventKey()` uses for v1's `eventRevisionRequested` setting.
- [x] **EVT-007** Add review event adapters where stable. — `ReviewSubmittedEventAdapter` (`submission.review_submitted`, new `SupportEventType` — v1 never had a review event, so nothing to migrate) built on the real, stable `PKP\submission\reviewAssignment\Repository::edit()`'s `ReviewAssignment::edit` hook, keyed on the review assignment's own id. Deliberately scoped to the single clearest, most support-relevant transition (a review was submitted); never includes reviewer identity (POL-009/010 discipline). Now wired to the real hook and enqueue path (EVT-019, live-accepted 2026-09-02) — record-only in the v2 queue until this type's own live-delivery ownership transfer (EVT-016B/EVT-020) completes.
- [ ] **EVT-008** Add payment event adapters where stable. — verified against a real local `pkp-lib` checkout: there is no `Hook::call`/`Hook::run` anywhere in `classes/payment/`. No stable hook exists to adapt; this item has nothing to build until pkp-lib adds one. Consistent with `PaymentSupportProviderInterface`'s existing on-demand-poll design rather than a payment event.
- [ ] **EVT-009** Add DOI event adapters where stable. — same finding as EVT-008: no `Hook::call`/`Hook::run` exists anywhere in `classes/doi/` in a real local `pkp-lib` checkout. Nothing stable to build.
- [x] **EVT-010** Delivery policy per journal/event. — `EventDeliveryPolicy::resolve()` + `EventDeliveryMode` implement the pure policy/filter decision (docs/v2/ARCHITECTURE.md §3.9's 5 modes), preserving v1's real `eventSyncMode` values (`note`/`open_update`) as the per-journal global default and adding per-event-type overrides v1 never had — already wired into the real delivery path via `v2EnqueueEvent()` (the earlier "not wired yet" note here was stale). ADM-004 closes the remaining admin-UI gap: `EventDeliveryPolicy::resolve()` now also accepts a real `EventDeliveryMode::*` constant directly as the global mode (not just the two legacy strings), and a new settings-form "Event Bridge" section gives an admin a real global-mode dropdown plus a JSON per-event-overrides field, resolved through the new pure `EventDeliverySettingsResolver`.
- [x] **EVT-011** Private note delivery. — `ChatwootIntegrationV2Plugin::deliverQueuedSupportEvents()`/`v2DeliverQueuedEventRow()` is the real consumer: pulls pending rows via `fetchPendingBatch()`, resolves the submission and its primary author at delivery time (`getPrimarySubmissionAuthor()` — author identity is never baked into the queued event itself, per EVT-003's own design), builds the message via `SupportEventMessageBuilder` (reusing v1's real locale strings), and posts a private note via `ChatwootApiService` — mirroring v1's real `sendChatwootEvent()` logic shape exactly. Driven by `DeliverQueuedSupportEventsTask`, registered every 5 minutes (far more frequent than the daily purge/Captain sync tasks, since a queued event shouldn't wait up to a day to send).
- [x] **EVT-012** Open/update conversation delivery. — same consumer: when the resolved delivery mode is `open_update_conversation` and no existing conversation is found for the contact, `v2DeliverQueuedEventRow()` creates one via the configured inbox (mirroring v1's real `open_update` branch exactly) — also creates the contact itself in that mode if one doesn't yet exist, matching v1's real fallback.
- [x] **EVT-013** Opt-in proactive message delivery. — `ChatwootApiService::createConversationMessage()` is the real, distinct Chatwoot messages endpoint (verified against a real `chatwoot/chatwoot` `develop` checkout — `messages_controller.rb` + `Messages::MessageBuilder`, `private: false` is what makes a message customer-visible, unlike `createConversationNote()`'s always-private `/notes` shortcut), and `v2DeliverQueuedEventRow()` handles `EventDeliveryMode::OPT_IN_CUSTOMER_MESSAGE` for real. Also closed `AUDIT_ONLY` while here (returns delivered without ever touching Chatwoot — the queue row's own lifecycle is the audit trail). ADM-004 closes the real "opt-in" consent mechanism itself: a new `eventDeliveryCustomerMessageConsent` settings-form checkbox is the only thing that can ever unlock `OPT_IN_CUSTOMER_MESSAGE` — `EventDeliverySettingsResolver` enforces this at the point of use (both for the global mode and any per-event override), silently downgrading to `PRIVATE_NOTE` whenever the mode is selected without the checkbox checked, so a customer-visible message can never be sent by accident or by a stale/malformed setting.
- [x] **EVT-014** Dead-letter/retry UI (first slice). — the underlying dead-letter/retry bookkeeping already existed in the queue schema itself (`attempts`/`run_after`/`last_error_code`/`status`, `DatabaseSupportEventQueueRepository::markFailed()`'s exponential backoff, same shape as v1's own retry queue) — a `failed` row past `max_attempts` is the dead letter, no separate table needed — exercised by the real delivery consumer (`deliverQueuedSupportEvents()`). ADM-005 closes the remaining admin-action half: a new `retryDeadLetters(int $contextId, int $limit)` repository method resets up to 50 real `failed` rows for one journal back to `pending` with a fresh attempts budget and no delivery delay (proven against a real `pdo_sqlite` database, same technique as TST-002/015 — real select-then-update, never an `UPDATE...LIMIT` some DB drivers don't support). A new "Retry Dead Letters" button in the Support Gateway Health section (ADM-002, which now also shows a real pending-event count alongside the dead-letter count) only appears when there is actually something to retry, and its response exposes only a count — never a row's `attributes`/`last_error_code` content, so no raw exception text or secret-bearing request body is ever surfaced. A dedicated per-row detail/timing view remains a separate, larger follow-up.
- [x] **EVT-015** Replay/duplicate tests. — the provable half is covered: duplicate prevention is fundamentally EVT-002's deterministic idempotency-key derivation (`tests/v2/support-event.php` — identical (type, contextId, resource, naturalKey) always derives the identical key, a retry at a different time still matches, any real difference never collides), which the DB's real unique constraint on `idempotency_key` (verified in `tests/v2/support-event-queue.php`) then enforces. Replay prevention is a structural fact also now asserted there: `fetchPendingBatch()` filters strictly on `status='pending'`, so a row `markDelivered()` already transitioned away from `pending` can never be fetched and delivered a second time. Full behavioral proof against a real duplicate-insert race or a real re-fetch-after-delivery requires a live DB, which this environment deliberately does not have (see `tests/v2/support-event-queue.php`'s own documented constraint) — this gap is now closed by TST-002's `tests/v2/tst-002-real-db-event-queue.php`, which proves both against a real `pdo_sqlite`-backed database.

## HOF — Human Handoff

- [x] **HOF-001** Define safe handoff summary DTO. — `HandoffSummaryFormatter::build()`.
- [x] **HOF-002** Include verification method/expiry safely. — reuses `SupportIdentitySerializer::serialize()` directly rather than re-deriving.
- [x] **HOF-003** Include verified relationship/resource. — only when a real, independently-resolved relationship exists.
- [x] **HOF-004** Include normalized support state/action. — only after independently re-checking `submission.read_own_support_status`/`read_own_required_actions`.
- [x] **HOF-005** Include safe recent event/diagnostic context. — `supportEscalateRequest()` now calls `SubmissionDiagnosticEngine::diagnoseRequiredAction($requiredActions)` internally, using the exact `$requiredActions` this same request already independently verified — never a caller-supplied diagnostic (Captain could claim any code). `HandoffSummaryFormatter::build()`'s new optional `$diagnostic` param folds `status`/`code`/`summary` into both the JSON summary and the rendered private-note text, following the same independently-re-checked pattern as every other fact in the summary.
- [x] **HOF-006** Explicitly exclude confidential review/editorial data. — reviewer identities/recommendations are never read; `HandoffSummaryFormatter` only arranges facts already proven safe elsewhere.
- [x] **HOF-007** Create/update private note idempotently. — `EscalationIdempotencyGuard`, best-effort/APCu-backed only (see API-016 note); not a durable cross-worker ledger.

## PRV — Provider Registry

Note: `ojs_get_payment_status` (API-012) was originally built directly against OJS's own native `OJSPaymentManager`/`OJSCompletedPaymentDAO` with no Provider Registry — there was exactly one real payment provider (OJS itself) to support at the time. A second, genuinely different provider now exists (Airix Submission Fee, verified against `Airix360/submissionFee-OJS` 1.7.0.0), so a minimal registry was built to support it — see `classes/v2/Provider/SupportProviderRegistry.php` and `docs/v2/AIRIX360_TASKLIST.md` AXP-*/APS-*. It is deliberately scoped to payment obligation providers only; the full Knowledge/Capability/Diagnostic/Event provider surface described in `AIRIX360_INTEGRATIONS.md` §4 remains unbuilt until a real provider needs each one.

- [x] **PRV-001** Define provider registry service. — `SupportProviderRegistry` (`registerPaymentProvider()`, `resolveObligations()`); payment-obligation providers only, not the full four-interface SDK.
- [x] **PRV-002** Define registration hook/API. — `discoverPaymentProviders()` fires `Hook::call('ChatwootIntegration::SupportProviders', [$registry])` so a sibling plugin can self-register without a hard-coded adapter (AXP-001); the first-party Airix Submission Fee provider is still detected/constructed directly by `Ojs35CompatibilityAdapter::getAirixSubmissionFeeProvider()`, not through the hook.
- [x] **PRV-003** Provider applicability/health contract. — `ProviderHealth::{AVAILABLE,DISABLED,NOT_INSTALLED,INCOMPATIBLE_VERSION,DEGRADED,UNAVAILABLE,UNKNOWN}`; the registry only calls `resolveObligation()` on a provider reporting `AVAILABLE`.
- [ ] **PRV-004** Provider capability/classification contract. — not started, deliberately: only `PaymentSupportProviderInterface` exists; `AccountSupportProviderInterface`/`SubmissionRequirementProviderInterface`/`ContributorSupportProviderInterface` (AIRIX360_INTEGRATIONS.md §4.2-4.4) remain undefined until a real, verified sibling plugin needs one — `SupportProviderRegistry`'s own docblock states this explicitly ("add each provider family only when a real, verified provider needs it"). Defining three unimplemented interfaces now would be exactly the premature abstraction this build has otherwise avoided.
- [x] **PRV-005** Reference example provider. — `AirixSubmissionFeeProvider`, verified against `Airix360/submissionFee-OJS` 1.7.0.0's public `PaymentHelper` surface (never its settings table directly).
- [x] **PRV-006** Provider isolation/failure tests. — `tests/v2/provider-registry.php`: a throwing provider, a non-`AVAILABLE` provider, and a null-returning provider must never prevent an unrelated real provider's obligation from resolving; a non-`AVAILABLE` provider's `resolveObligation()` must never even be called.
- [x] **PRV-007** Document third-party integration guide. — `docs/v2/PROVIDER_INTEGRATION_GUIDE.md`: real hook usage, the `PaymentSupportProviderInterface` contract, what the registry does with a resolved obligation, what a provider must never do, `AirixSubmissionFeeProvider` as the reference implementation, and an explicit scope note pointing at PRV-004 for why the other three provider families aren't documented (they don't exist yet).

## MCP — MCP Adapter

- [x] **MCP-001** Select protocol/library implementation strategy. — decided and recorded in `docs/v2/ADRS.md` ADR-023: MCP revision `2026-07-28`, stateless Streamable HTTP transport (no deprecated HTTP+SSE, no STDIO), custom protocol layer isolated under `classes/v2/Mcp/` rather than a required `mcp/sdk` runtime dependency (pre-1.0/experimental; this plugin ships no runtime Composer tree today). Initial scope: `server/discover`/`tools/list`/`tools/call`/`resources/list`/`resources/read` only. Transport foundation built: `McpProtocol` (revision + supported-method allowlist), `McpErrorCode` (JSON-RPC 2.0 codes + 3 MCP-specific ones), `McpRequest`/`McpResponse` (value objects), `McpRequestParser` (malformed JSON/wrong-shaped request/missing-or-wrong protocol revision all fail deterministically, never guessed), `McpDispatcher` (routes only methods in the supported-method allowlist, wraps handler exceptions into a generic error that never leaks internal detail, refuses to even register a handler for an unsupported method name). `tests/v2/mcp-protocol.php` covers test requirements 2/3/4 from ADR-023's list (malformed JSON, unsupported protocol revision, unknown method all fail safely). No tool/resource registry, no auth layer, and no Support Core wiring yet — that begins with MCP-002/003.
- [x] **MCP-002** Define MCP authentication/client model. — `McpAuthenticator` (own distinct entry point, deliberately not just a Support API call site reused, even though it shares `ServiceTokenAuthenticator`'s generic Bearer-match algorithm — the credential *namespace* it must never share is the configured secret value, per ADR-023). `McpPublicConsumer::baseCapabilityRequest()` defines the public MCP consumer model: always `CONSUMER_MCP_PUBLIC_SUPPORT` at `v0` (unverified) — proven to unlock exactly the same baseline capability set an unverified Chatwoot Captain caller gets, never more, and proven that an authenticated-but-unrelated MCP context still cannot reach a resource-scoped capability without an actually-proven relationship. No page handler/endpoint wiring yet (that's MCP-003) — this establishes the credential check and the base authorization floor a real MCP tool call will start from.
- [x] **MCP-003** Implement public/read MCP tool set. — first slice: real page handler (`McpGatewayPageHandler` -> `ChatwootIntegrationV2Plugin::mcpRequest()`) wired at the `ojsMcpGateway` page, authenticating via `McpAuthenticator` against a distinct `mcpServiceToken` setting *before* the request body is ever parsed (so an unauthenticated caller can never use parse errors as a probing oracle) — `mcpServiceToken` is deliberately absent from `LEGACY_EXPORT_KEYS`, so it can never be exported either (ADR-021). `McpToolRegistry` (built fresh per request, never a singleton) + `McpHandlerError` (lets a handler report a specific error code like `UNKNOWN_TOOL_OR_RESOURCE` without collapsing to a generic internal error) + first real tool `journal.get_profile` (`JournalProfileTool`), which reuses the exact same already-vetted Knowledge Compiler output the REST `/support-knowledge/` pages serve — never a second parallel path to journal facts, never a raw `KnowledgeFact` object or provenance metadata in the tool result. `tools/list`/`tools/call` are the only methods registered so far; `server/discover`/`resources/*` remain unregistered-but-supported (McpDispatcher reports `METHOD_NOT_FOUND`, never a fatal). Two more no-identity public tools added: `journal.get_submission_policy` (`SubmissionPolicyTool`, the real `submission.*` facts) and `journal.get_fee_policy` (`FeePolicyTool`, the real `fee.*` facts) — same reuse pattern as `journal.get_profile`.

The remaining ADR-023 tools (`identity.*`/`submission.get_support_status`/`submission.get_required_actions`/`payment.get_submission_status`/`publication.get_status`/`diagnostics.*`/`support.escalate`) all require resolving a specific end-user's identity/relationship, which in the real REST Support Core means resolving a `SupportSession` bound to a (chatwootAccountId, chatwootContactId, chatwootConversationId) tuple. That tuple is not MCP transport state (MCP-001's statelessness) — it is real Chatwoot conversation-binding data `SupportApiRequestResolver` already accepts as explicit parameters (not derived from the transport), so an MCP tool call that needs identity can simply carry the same three IDs as tool arguments, exactly like REST already carries them as POST fields. No new session model is needed. The reuse work is now built: `identity.get_support_identity` (`SupportIdentityTool`) is the first identity-dependent tool, resolving identity through the exact same `SupportApiRequestResolver` REST uses (reading the Chatwoot conversation tuple from tool arguments), passing the distinct `mcpServiceToken` as the resolver's configured-token parameter (never `chatwootSupportApiToken`), and reusing `SupportIdentitySerializer::serialize()` verbatim — REST/MCP response equivalence (MCP-006) by construction, not by convention. `McpSupportApiFailureMapper` maps every real `SupportApiErrorCode` to a specific `McpErrorCode` (added `RATE_LIMITED`) rather than a blanket fallback, so a future identity-dependent tool never has to reinvent this. `submission.get_required_actions` (`RequiredActionsTool`) follows the same pattern, additionally introducing two shared private helpers on the plugin (`v2ResolveMcpIdentity()`, `v2ResolveMcpSubmissionContext()`) that mirror the identity-resolution and relationship/capability-resolution steps every submission-scoped REST endpoint already repeats — real, discovered duplication extracted once real repetition is about to happen a second time, not a speculative refactor. Capability evaluation for MCP tools now correctly runs under `CONSUMER_MCP_PUBLIC_SUPPORT` (MCP-002), never the Chatwoot Captain consumer plane. `submission.get_support_status` (`SubmissionSupportStatusTool`) follows immediately, reusing `SubmissionSupportSerializer` and the same shared helpers — both submission-scoped tools now resolve through `v2ResolveMcpSubmissionContext()`, never a bespoke copy each. `publication.get_status` (`PublicationStatusTool`) follows, reusing `PublicationStatusSerializer` and the same conservative doi/publicUrl/issue gating REST already applies (only populated for `published`/`scheduled_for_publication`, `publicUrl` restricted to `published` only, issue metadata only when `getIssueInfo()` itself reports the issue published). `payment.get_submission_status` (`PaymentStatusTool`) follows, reusing `PaymentStatusSerializer` and the same shared `v2ResolvePaymentObligations()` provider-resolution helper REST uses — including the real Airix-provider-precedence rule (a registered provider's obligation, when present, is authoritative over the native OJS publication fee; a provider that genuinely failed reports `unknown`, never a silent fallback). `v2ResolveMcpSubmissionContext()` now accepts an optional feature-flags parameter so this tool can pass the real `payment_status` flag into capability evaluation, same as REST. All four submission-scoped tools now resolve through this one shared helper. `diagnostics.account` (`AccountDiagnosticsTool`) follows, reusing `DiagnosticResultSerializer`/`AccountDiagnosticEngine` verbatim — no submission relationship involved, so it builds its own capability request directly rather than through `v2ResolveMcpSubmissionContext()`. `diagnostics.submission` (`SubmissionDiagnosticsTool`) follows, reusing the same `SubmissionDiagnosticEngine` scope-dispatch match REST uses verbatim, plus the two shared private helpers (`supportStateForDiagnostics()`, `diagnosePaymentForSubmission()`) REST already had — the latter extended to accept a `$consumerPlane` parameter (defaulting to the Chatwoot Captain plane for REST's existing call site unchanged) so MCP's payment-diagnostic scope correctly passes `CONSUMER_MCP_PUBLIC_SUPPORT`. All five submission-scoped tools now resolve through `v2ResolveMcpSubmissionContext()`. `support.escalate` (`EscalateSupportTool`) follows, mirroring REST's `ojs_escalate_support` line-for-line (deliberately V0/unauthenticated-capable, same as REST — a human handoff must remain available even when verification itself is failing): gates on `support.escalate` under `CONSUMER_MCP_PUBLIC_SUPPORT`, then independently re-checks each optional submission-scoped fact against its own dedicated capability exactly as REST does (not through `v2ResolveMcpSubmissionContext()`, since REST's own escalate handler never used that shared helper either — no forced extraction over an already-bespoke REST reference), builds the summary via the real `HandoffSummaryFormatter::build()`/`SupportIdentitySerializer::serialize()`, and posts the Chatwoot private note through the real `EscalationIdempotencyGuard`/`ChatwootApiService::createConversationNote()` — the same downstream Chatwoot API credential (`chatwootApiAccessToken`) REST already uses for this, which is not the MCP transport credential and was never claimed to be. `submission.list_mine` (`SubmissionListTool`) follows, mirroring REST's `supportSubmissionListRequest()` (`GET /ojsSupportGateway/submissions`) — the first MCP tool with no ADR-023 identity/relationship precedent but a real, already-built REST reference (`submission.list_own` capability, `RuntimeContextBridge::listCandidateSubmissions()`, `SubmissionListSerializer`, `PaginationParams`): a broad, safe OJS-native candidate query supplies only candidates, never authorization, and every candidate is independently re-checked through the same `resolveSubmissionRelationship()` other tools use, keeping only author/reviewer results (editorial-only relationships excluded, same as REST). Capability evaluation runs under `CONSUMER_MCP_PUBLIC_SUPPORT`; a denied/unverified caller gets the exact same generic empty shape as REST (never a distinguishable error, so a denied capability can't be enumerated). `capabilities.list_available` (`CapabilitiesListTool`) follows, mirroring REST's `supportActionsRequest()` (`GET /ojsSupportGateway/actions`) — capability discovery separated from identity/status, same verified/assurance/availableActions/disabledActions shape REST builds inline (no dedicated REST serializer class exists for this either, so the tool mirrors that directly rather than inventing one). Capability evaluation runs under `CONSUMER_MCP_PUBLIC_SUPPORT`, same as every other MCP-003 tool. This completes ADR-023's initial public tool list — MCP-004 (Knowledge Compiler resources) is next.
- [x] **MCP-004** Implement safe public resources. — `resources/list`/`resources/read` are now registered on `McpDispatcher` (previously unregistered-but-supported, reporting `METHOD_NOT_FOUND`). New `McpResourceRegistry` mirrors `McpToolRegistry`'s shape exactly (built fresh per request; `list()` exposes only uri/name/description/mimeType, never the content handler — same MCP-011 non-leak guarantee). Three resources under the `ojs://journal/{contextId}/...` hierarchy per ADR-023/API_MCP_SPEC.md §11: `support-profile`, `submission-guidelines`, `fee-policy` — each sources its content by calling the exact same already-built `JournalProfileTool::handle($compilation)`/`SubmissionPolicyTool::handle($compilation)`/`FeePolicyTool::handle($compilation)` the equivalent tools use, never a second parallel path to journal/submission/fee facts. Content is Knowledge-Compiler-only, as ADR-023 requires — no submission/user-specific live state is ever exposed as a public static resource.
- [x] **MCP-005** Separate staff tool namespace/credential. — satisfied structurally, per ADR-023: `CapabilityRequest::CONSUMER_MCP_STAFF` exists as a reserved constant, but no staff/editorial capability is defined anywhere in `CapabilityCatalog` and `mcpRequest()` never references `CONSUMER_MCP_STAFF` at all — the public MCP gateway has no route to a staff plane, not merely a policy denial. Now proven in `tests/v2/mcp-security.php` rather than left as an implicit design claim.
- [x] **MCP-006** REST/MCP equivalence contract tests. — new `tests/v2/mcp-security.php` proves byte-identical output between REST's call site (`RequiredActionsSerializer::verified()`) and MCP's call site (`RequiredActionsTool::handleVerified()`) for `submission.get_required_actions`/`ojs_get_required_actions` given the identical resolved relationship/actions fixture — the concrete mechanism behind "equivalence by construction," not merely two call sites that happen to share a name.
- [x] **MCP-007** Verify public client cannot reach staff capability. — `tests/v2/mcp-security.php` proves this three independent ways: (1) `CapabilityCatalog::all()` contains no staff/editorial capability; (2) `CONSUMER_MCP_STAFF` resolves to the exact same allowed-capability set as `CONSUMER_MCP_PUBLIC_SUPPORT` for an identical identity/relationship (the consumer tag alone confers no extra authority — `CapabilityPolicyEngine`/`CoreCapabilityProvider` never branch on consumer at all); (3) `mcpRequest()`'s method body never references `CONSUMER_MCP_STAFF`, so the public endpoint cannot even construct a staff-plane request.
- [x] **MCP-008** OpenClaw integration test. — scoping decision: "OpenClaw" in `docs/v2/BUILD_PLAN.md`/`docs/v2/ARCHITECTURE.md` names one example of a generic external MCP client/agent, not a dedicated OpenClaw SDK or wire format anywhere in this codebase or its docs (confirmed by search) — building an OpenClaw-specific API here would test something that does not exist. New `tests/v2/mcp-openclaw-integration.php` instead proves what actually matters: a real, spec-compliant generic MCP client can complete a full round trip against the actual `McpRequestParser` → `McpDispatcher` pipeline `mcpRequest()` assembles per request, using only raw JSON-RPC bytes over the wire (never a PHP-level shortcut into the registries) — `tools/list` → `tools/call` (including an unknown-tool failure with a correlatable id/error code) → `resources/list` → `resources/read` → a wrong-protocol-revision rejection. One representative tool/resource is enough for this; per-tool business logic is already covered in `tests/v2/mcp-tools.php`/`mcp-identity.php`. This is MCP's last slice (PR G) before MCP docs/tasklist reconciliation.
- [x] **MCP-009** Document that Chatwoot-native MCP is not required. — verified against a real chatwoot/chatwoot checkout: there is no MCP client/server code anywhere in it (`rg -l 'model.context.protocol|\bmcp\b'` over the Ruby source returns nothing outside this integration's own docs). Chatwoot's real Captain Custom Tools integrate over plain remote-endpoint HTTP (`CanonicalToolDefinition`'s endpoint URL + auth token, already built/provisioned in this plugin) — never MCP. This is confirmed by a real independent precedent already running in production: a standalone Node.js MCP server (`@modelcontextprotocol/sdk`) that gives an external agent scoped access to Chatwoot's `Captain::AssistantResponse` data by shelling out to `docker exec rails runner` — i.e., MCP access to Chatwoot is always a separate adapter built *around* Chatwoot from the outside, never a feature Chatwoot ships itself. `docs/v2/ARCHITECTURE.md` §3.13 already states this design intent ("no assumption that Chatwoot itself is the MCP client"); this entry closes MCP-009 by recording the concrete verification behind that statement. Consistent with everything else in the MCP-* section (MCP-001 through MCP-008) remaining open: this build has never needed to add its own MCP adapter, since the real, verified integration surface (Captain Custom Tools) already works fully over the existing REST Support API.

## ADM — Admin Console

- [x] **ADM-001** Admin settings foundation: masked secrets + MCP credential field (first slice, not separately numbered in the original backlog — added once the admin console work was authorized). Reorganizes `ChatwootSettingsForm`/`templates/settingsForm.tpl` into labeled Connection/Support API/MCP sections and gives `mcpServiceToken` its first-ever settings-form field (closing the gap `docs/v2/MCP_SETUP_GUIDE.md` §3 previously documented — an admin no longer needs `Plugin::updateSetting()` from a one-off script). New shared `classes/v2/Settings/SecretFieldMasking` (pure, fully unit-tested logic) is applied to every real secret field (`chatwootIdentityValidationSecret`, `chatwootApiAccessToken`, `chatwootSupportApiToken`, `mcpServiceToken` — deliberately not `chatwootBaseUrl`/`chatwootWebsiteToken`/`chatwootInboxId`, which are not secrets): a saved secret always displays as a fixed mask, never its plaintext value; resubmitting the mask unchanged keeps the real stored secret; submitting a new value replaces it; submitting an explicit empty value clears it. Rendered as real `type="password"` fields, never plaintext-echoed text. `mcpServiceToken` is confirmed absent from both `LEGACY_EXPORT_KEYS` (v2) and `EXPORT_KEYS` (v1, which also gates `importSettings()`), so it remains structurally impossible to export or import via the settings backup path — never written into a log by this form either, since the form never logs anything. The admin's MCP section also displays the real MCP endpoint URL (built from the real `ojsMcpGateway` page route, never hardcoded) and protocol revision.
- [x] **ADM-002** Unified Support Gateway health section (first slice, not separately numbered in the original backlog). New pure `SupportGatewayHealthAggregator`/`SupportGatewayHealthSummary` (`classes/v2/Health/`) consolidate every already-built health signal into one deterministic overall state (`healthy`/`degraded`/`failed`) — the aggregator itself fetches nothing and makes no network call, so it is fully unit-tested for every rule (unconfigured Chatwoot or a failed Knowledge/provider signal always fails; a degraded Knowledge/Captain state, a missing (but not itself failed) credential, or accumulating dead letters each independently degrade; Captain's own `not_provisioned` state is treated as neutral, never evidence of a problem; a genuinely-not-applicable provider state — disabled/not installed/incompatible — never counts as a failure; nothing is ever reported healthy without a real signal saying so). New plugin method `supportGatewayHealthSummary()` gathers the real inputs — `chatwootBaseUrl`/`chatwootWebsiteToken`/`chatwootIdentityValidationSecret`/`chatwootApiAccessToken`/`chatwootInboxId` configured (reusing the existing `supportGatewayUsable()` check), `chatwootSupportApiToken`/`mcpServiceToken` configured, mail transport configured (reusing the real `AccountDiagnosticEngine::diagnose(SCOPE_MAIL_CONFIGURATION, ...)` diagnostic verbatim), the real `KnowledgeHealthService`/`CaptainProvisioningHealthService` reports, the real Airix payment provider's own `health($context)`, and a new `SupportEventQueueRepositoryInterface::countByStatus()` for the real dead-letter count — every one of these is a local/OJS-internal read, never a live Chatwoot/Captain HTTP call, so it is safe on every admin page render. Rendered as a read-only summary in the settings form's new "Support Gateway Health" section. This closes or materially advances the health-reporting half of CWO-008/CWO-013/KNO-020/EVT-014 (a real, consolidated, tested signal now exists and is surfaced) — the remaining admin-actionable halves of those (manual Captain repair, per-event delivery-mode configuration, opt-in customer-visible messaging consent, a safe dead-letter retry action) are separate, larger follow-up slices, not attempted here.
- [x] **ADM-003** Manual "Sync/Repair Captain" admin action (first slice, not separately numbered in the original backlog), closing the on-demand manual-fallback trigger `provisionCaptainKnowledgeDocument()`/`provisionCaptainCustomTools()`/`provisionCaptainScenarios()` always documented as missing. Deliberately zero new provisioning logic: new `ChatwootIntegrationV2Plugin::syncCaptainResources()` only orchestrates the three already-built, already-tested entry points, in the same document-then-tools-then-scenarios order `CaptainSyncScheduledTask` already uses (a scenario can only reference a tool by its real assigned slug), scoped to the current journal only — never every journal system-wide like the scheduled task. The never-mutate-unrelated-resources guarantee is untouched: this method has no independent Chatwoot access at all (never constructs its own `ChatwootApiService`/sync repository), so every ownership/conflict check still comes entirely from the three existing provisioners. Wired as a new `syncCaptainResources` verb (`ChatwootIntegrationV2Plugin::manage()` now overrides v1's dispatch to add it) and a "Sync/Repair Captain" button in the settings form's Support Gateway Health section. This closes the admin-action half of CWO-013's health report (ADM-002 closed the reporting half) and materially advances KNO-019's manual half.
- [x] **ADM-004** Event Bridge policy configuration UI (first slice, not separately numbered in the original backlog), closing EVT-010's remaining admin-UI gap and EVT-013's real opt-in consent mechanism. `EventDeliveryPolicy::resolve()` now also accepts a real `EventDeliveryMode::*` constant directly as the global mode (previously only the two legacy `note`/`open_update` strings), backward-compatible with every existing caller/test. New pure `EventDeliverySettingsResolver` (`classes/v2/Event/`) is the one place the "customer-visible messages require explicit opt-in, never silently enabled" rule is enforced — for both the global mode and any per-event override, `OPT_IN_CUSTOMER_MESSAGE` is only ever honored when the new `eventDeliveryCustomerMessageConsent` setting is checked, otherwise it silently downgrades to `PRIVATE_NOTE`; a malformed/unrecognized per-event-overrides JSON field fails closed to no overrides, never crashes delivery. New settings-form "Event Bridge" section: a global-mode dropdown (with a "use the legacy Sync Mode setting" default, preserving existing installs' behavior unchanged until an admin deliberately opts in), the consent checkbox, and a JSON per-event-overrides field. `v2EnqueueEvent()` now resolves through this resolver instead of reading `eventSyncMode` directly.
- [x] **ADM-005** Dead-letter retry admin action (first slice, not separately numbered in the original backlog), closing EVT-014's remaining admin-action half. New `SupportEventQueueRepositoryInterface::retryDeadLetters(int $contextId, int $limit): int` (proven against a real `pdo_sqlite` database) resets up to `$limit` real `failed` rows for one journal back to `pending` — fresh attempts budget, no delivery delay — via a portable select-then-update (never an `UPDATE...LIMIT`, which not every supported DB driver allows). Scoped to the current journal only, same precedent as ADM-003. New plugin method `retryDeadLetterEvents()` delegates to it and returns only a count — never a row's `attributes`/`last_error_code` content — wired as a new `retryDeadLetterEvents` verb and a "Retry Dead Letters" button that only appears in the Support Gateway Health section (ADM-002) when `deadLetterCount > 0`. `SupportGatewayHealthSummary` now also surfaces a real pending-event count alongside the existing dead-letter count.
- [x] **ADM-006** "Send test email" admin diagnostic (first slice, not separately numbered in the original backlog), deliberately scoped down from the full verification-EmailTemplates migration after real primary-source research against a local pkp-lib checkout (`stable-3_5_0`) showed the full migration is substantially larger than a single slice — exactly the scenario the owner directive itself anticipated ("if the real EmailTemplate registration model turns out to be substantially more involved than expected, it is fine to scope this PR to research + a smaller first slice... and document the EmailTemplate migration as a following slice"). New `SupportMailTestMailable` (`classes/v2/Verification/`) is a dedicated `PKP\mail\Mailable` subclass, structurally independent of `SupportVerificationMailable`/`VerificationChallengeService` (no shared pepper/challenge code, never a wrapper or subclass) so a change to either can never affect the other. New plugin method `sendSupportMailTest()` sends it to the current admin's own account via the same real `Mail::send()` transport every other plugin email uses, wrapped in try/catch — success only ever means "OJS handed the message to its configured transport," never a claim of actual inbox delivery (this codebase has no visibility into delivery, same limitation already documented on `AccountDiagnosticEngine`). Wired as a new `sendSupportMailTest` verb and an unconditional "Send Test Email" button in the Support Gateway Health section.
  - **Follow-up slice, precisely specified from real pkp-lib source (not guessed), deferred per the owner directive's own permission to scope down:** promoting the two verification emails (PIN, secure link) into real, admin-configurable OJS EmailTemplates. Confirmed facts from `pkp-lib` `stable-3_5_0`: (1) core's default templates are seeded from a fixed core XML registry (`registry/emailTemplates.xml`) during a core upgrade migration — a plugin cannot append to this without a forbidden core modification; (2) the real plugin-accessible path is `Repo::emailTemplate()->add(EmailTemplate $emailTemplate)` (`classes/emailTemplate/Repository.php`), which lets a plugin create its own custom `email_templates`/`email_templates_default_data` rows directly, no core registry involved; (3) the real Mailable pattern for an admin-configurable template (see `classes/mail/mailables/PasswordResetRequested.php`) uses the `Recipient`/`Configurable` traits plus a `protected static ?string $emailTemplateKey`, with variables added via `$this->addData([...])` and documented via a static `getDataDescriptions()`; (4) the base `Mailable`/`Configurable` trait does **not** auto-resolve its own template — the real call-site pattern (confirmed in `classes/invitation/invitations/userRoleAssignment/UserRoleAssignmentInvite.php` ~L90-118) is for the *sending* code to look up `Repo::emailTemplate()->getByKey($context->getId(), $mailable::getEmailTemplateKey())` itself, read `getLocalizedData('subject'/'body', $locale)`, and apply it to the Mailable before `Mail::send()`. This slice would need: a new Illuminate migration inserting the two `email_templates_default_data` rows scoped to this plugin (never touching core's registry), two new `Configurable`/`Recipient`-based Mailable subclasses replacing the current fixed-content `SupportVerificationMailable`, and rewiring the real verification-sending call site to look up and apply the template — plus a locale-variable-safety review confirming only the narrow allowed variables (journal name, verification code, expiry, verification URL) are ever exposed, never submission/reviewer/Chatwoot/session identifiers or secrets.

## AUD — Audit & Observability

- [x] **AUD-001** Audit migration/repository. — `chatwoot_support_audit_log` table (`InstallSupportGatewayMigration::upAuditLog()`) + `DatabaseSupportApiAuditLogger`, now the default sink `SupportApiRequestResolver` constructs; `ErrorLogSupportApiAuditLogger` remains as a fallback-only implementation, no longer wired as the default. Query/browse UI not built (not required by AUD-001's own scope; see AUD-008 for a dashboard).
- [x] **AUD-002** Correlation IDs across REST/events/Chatwoot. — 2026-09-02: now threaded through the event/queue/delivery side too (see AUD-013), not just the Support API request side. 2026-09-03: Captain sync and legacy dead-letter give-up also now carry real correlation IDs into the same shared audit table (see AUD-013).
- [x] **AUD-003** Verification lifecycle audit. — same work as IDN-019 (`v2AuditVerificationEvent()`), tracked under two IDs in this document; see IDN-019 for the real implementation (request/PIN-confirm/link-confirm outcomes recorded through `DatabaseSupportApiAuditLogger`, reason codes only, never a PIN/token/email, never changing the caller-facing anti-enumeration response).
- [x] **AUD-004** Protected read allow/deny audit. — `SupportApiRequestResolver` records every allow/deny decision (with reason code) through `SupportApiAuditLoggerInterface`.
- [ ] **AUD-005** Staff mutation audit.
- [x] **AUD-006** Secret/PII log redaction tests. — `DatabaseSupportApiAuditLogger::record()` allowlists exactly the fields the resolver ever emits (`correlationId`/`endpoint`/`contextId`/`decision`/`reason`/`assurance`); an unrecognized key is silently dropped, including on the DB-failure `error_log()` fallback path (fixed a real leak: the fallback previously logged the raw un-allowlisted `$event`, not the allowlisted row — caught by `tests/v2/audit-logger.php`). Scope is limited to this one sink; no redaction tests exist yet for other log call sites in the plugin.
- [x] **AUD-007** Configurable retention/purge. — `PurgeExpiredSupportDataTask` now also purges audit rows past a 90-day retention window (`DatabaseSupportApiAuditLogger::purgeOlderThan()`), registered via the same daily `HasTaskScheduler` schedule as session/challenge purging. Retention is a class constant, not an admin-configurable setting — "configurable" here means "one code-level knob," not a settings-UI field; revisit if a real need for a per-journal retention setting emerges.
- [ ] **AUD-008** Health dashboard for components/providers/queues.

## TST — Test Program

- [ ] **TST-001** Unit test suite. — partial: 69 files under `tests/v2/*.php` cover nearly every category `docs/v2/TEST_PLAN.md` §3 "Unit" lists (capability, verification, diagnostics, serializers/field-allowlists, event idempotency, knowledge classification/fingerprint). No dedicated `SupportStateMapper` unit test file — it is only exercised incidentally as fixture input inside other tests.
- [x] **TST-002** OJS integration test harness (first slice). — scoping decision: a full real-OJS-runtime + MySQL/Postgres harness would require introducing a Composer dependency tree, CI service containers, and a full pkp-lib checkout — genuine new infrastructure, not something one incremental PR should add speculatively (do-not-overbuild). What's actually built instead closes the specific, previously-flagged gap: `tests/v2/tst-002-real-db-event-queue.php` uses PHP's bundled `pdo_sqlite` (now installed in CI alongside `php-cli` in `.forgejo/workflows/ci.yml` — a one-package addition, no service container) to run real SQL against a real, running relational database engine. It replicates the event-queue table's exact schema from the real migration (verified in-test against the migration source, so it can never silently drift into a fabricated schema) and proves, against that real database, the two EVT-015 claims that previously could only be asserted at the source level: a real concurrent duplicate-insert race resolves to "already queued" via the real unique constraint (never a crash, never two rows), and a row already transitioned away from `status='pending'` can never be re-fetched by a real status-filtered query — plus the `run_after` backoff window's visibility is honored by a real query, not just present in the schema. A full MySQL/Postgres + live pkp-lib harness remains future scope.
- [x] **TST-003** Chatwoot mock/contract harness (first slice). — scoping decision: `ChatwootApiService` cannot be instantiated in this environment at all — its constructor immediately builds a `GuzzleHttp\Client` and makes a real HTTP call (`resolveAccountId()` -> `getProfile()`), and this repo has no Composer/vendor tree (confirmed: no `composer.json`, no `vendor/`, `class_exists('GuzzleHttp\Client')` is false) — Guzzle is only ever available inside a real OJS core install, which is why every existing test touching this class only ever does source-level `str_contains()` inspection. Introducing Guzzle/Composer here to test the class directly would be new infrastructure, not a first slice (do-not-overbuild). What's built instead: `tests/v2/tst-003-chatwoot-contract.php` starts a real local HTTP server (PHP's built-in `php -S`, no extension/dependency needed) that speaks the exact endpoints/payload shapes `ChatwootApiService.php` declares (verified in-test against that source so the mock can never silently drift), and sends real HTTP requests over a real socket (PHP's built-in stream wrapper, no Guzzle needed) built the same way Guzzle would — same headers, same JSON body shape — for `getProfile()`, `getConversation()`, and `createConversationNote()` (the method the real `support.escalate` handoff uses). Each real request the mock server actually receives on the wire is asserted against, not just the client's intent. Also proves a real connection-refused failure resolves to a non-blocking `ok=false`, mirroring `requestJson()`'s own catch contract (ADR-016). No CI infrastructure change was needed for this slice (unlike TST-002's `php-sqlite3`) — PHP's stream wrapper and built-in server require no extra package.
- [x] **TST-004** OJS 3.5 exact-version matrix (first slice, real runtime proof — RUN-001). Verified against the team's actual real, live OJS 3.5.0-5 + MySQL 8.0 install (`ojs-demo.airixmedia.com/ajdsi`, docker-compose on host `dell`, the team's standing testing ground — not a synthetic/ephemeral CI-only environment) rather than reinventing a from-scratch bootstrap: the plugin repo is bind-mounted there and had drifted to an old v2-dev commit (`d39ed6b`) with the still-1.0.0.2 `version.xml`, registered `current=1` in the real `versions` table. Fast-forwarded the mounted checkout to `v2-dev` HEAD (`634d314`, includes the `2.0.0.0` version bump) and ran OJS's real, documented single-plugin upgrade tool, `lib/pkp/tools/installPluginVersion.php plugins/generic/chatwootIntegration/version.xml`, inside the live container. Confirmed against the real database: the `versions` table now shows `chatwootIntegration` at `2.0.0.0` with `current=1` (old `1.0.0.2` row now `current=0`), and all five real Support Gateway tables exist (`chatwoot_support_sessions`, `chatwoot_support_verification_challenges`, `chatwoot_support_knowledge_sync`, `chatwoot_support_audit_log`, `chatwoot_support_event_queue`), created by the real, idempotent `InstallSupportGatewayMigration` — not asserted, actually created via the real migration run. Confirmed the live site kept serving real anonymous traffic normally throughout (index/about/search/issue-archive/login all `200`, before and after the upgrade) with zero fatals/errors/exceptions in the container log across the whole window — the shared demo, which other in-flight work also depends on, was not disrupted. Scope is deliberately narrow: this proves a real install/upgrade cycle succeeds cleanly on genuine OJS 3.5.0-5 + MySQL; it does not yet cover PostgreSQL (TST-007), a from-scratch fresh install (only an upgrade-from-v1 path was exercised here), or authenticated admin-UI verification of the new Support Gateway settings sections (deferred — no credentials were entered by the agent).
- [ ] **TST-005** OJS 3.6 exact-version matrix.
- [ ] **TST-006** Supported MySQL/MariaDB matrix where practical. — MySQL 8.0 partially covered as a side effect of TST-004's real upgrade run (`ojs-demo.airixmedia.com`'s real `mysql:8.0` container) — real install/upgrade/migration succeeded cleanly against it. MariaDB not yet exercised; not claiming this item done.
- [ ] **TST-007** Supported PostgreSQL matrix where practical.
- [x] **TST-008** PHP matrix derived from target OJS requirements. Verified against a real local `pkp-lib` `stable-3_5_0` checkout (not `main`, which already reports 3.6.0.0): `composer.json` requires `"php": "^8.2"`, `PKPApplication::PHP_REQUIRED_VERSION = '8.2.0'`, and `pkp/ojs`'s own `stable-3_5_0` CI matrix tests PHP 8.2/8.3 — never 8.1. PHP 8.1 was never a supported OJS 3.5 target; `.forgejo/workflows/ci.yml` and `.github/workflows/ci.yml` are corrected from `8.1`/`8.2` to `8.2`/`8.3`, matching OJS 3.5's own real matrix exactly. Findings recorded in `docs/v2/VERIFICATION_MATRIX.md`'s new "PHP support matrix" section.
- [x] **TST-009** Multi-journal tests. — `tests/v2/context-resolver-isolation.php` directly proves cross-journal isolation for `ContextResolver`: the identical numeric user id under two different journals never shares role evidence, journal id/path never swap between two independent `resolve()` calls, and locale never leaks between them. Scope is narrow (`ContextResolver` only, not e.g. `KnowledgeCompiler` cross-journal isolation) — a real gap for a future slice, not claimed here.
- [x] **TST-010** Multi-role tests. — `tests/v2/relationship.php` proves author-only/reviewer-only/both/neither differentiation with real evidence objects; `tests/v2/required-actions.php` proves `RequiredActionMapper::forAuthor()`/`forReviewer()` diverge correctly per support state. Editor/staff role is correctly out of scope — no staff plane exists yet (POL-004 unstarted).
- [x] **TST-011** Blind-review/privacy tests. — `tests/v2/blind-review-anonymity.php` proves reviewer A never sees reviewer B's assignment status/JSON, and the author's serialized response never reveals reviewer identity/count/existence — checked at the actual serialized-JSON level, not just object state.
- [x] **TST-012** Verification abuse tests. — `tests/v2/external-verification.php` covers brute-force lockout after `maxAttempts` (rejects even the correct PIN once locked out), replay of an already-consumed PIN, expired/revoked challenge rejection, and resend-cooldown throttling. `tests/v2/forged-chatwoot-header.php` separately proves conversation verification checks Chatwoot's own `hmac_verified` field, never a caller claim.
- [x] **TST-013** API contract tests. — `tests/v2/openapi-contract.php` (API-018/ACC-001/ACC-001B) bidirectionally checks all 14 operations' real success- and error-response shapes against `openapi.json`, plus the path-existence check.
- [x] **TST-014** Upgrade from v1.0.0.2 test — real bug found live and fixed. Real upgrade exercised against `ojs-demo.airixmedia.com` (see RUN-001/TST-004) surfaced a genuine, severe bug: post-upgrade, every new v2 HTTP route (`ojsSupportGateway/*`, `ojsMcpGateway`, `support-knowledge/*`) 404'd, while v1 features (widget injection, all normal pages) and an unrelated plugin's own custom page on the same journal (`contributorApproval/confirm`) worked fine — ruling out a site-wide routing problem. Root cause, confirmed via real pkp-lib source (`classes/plugins/PluginRegistry.php`) plus a local PHP reproduction (not further live trial-and-error): `PluginRegistry::instantiatePlugin()` — the path every real page dispatch uses, via `Dispatcher::dispatch()` -> `loadCategory('generic', true)` -> `loadFromDatabase()` — instantiates a plugin by **guessing its class name from the installation directory** (`"\APP\plugins\{$category}\{$pluginName}\" . ucfirst($pluginName) . 'Plugin'`), never through this repo's own `index.php` wrapper. Composer's real PSR-4 mapping (`"APP\\plugins\\": "../../plugins/"`, confirmed in `pkp-lib/composer.json`) means that guessed FQCN resolves straight to a file on disk regardless of `index.php` — and before this fix, `APP\plugins\generic\chatwootIntegration\ChatwootIntegrationPlugin` was the v1-only class, so real page dispatch always ran v1 alone; `index.php`'s `ChatwootIntegrationV2Plugin` was never reached in production at all. (A secondary, related correctness issue confirmed along the way: `LazyLoadPlugin::getName()`'s default — `strtolower(get_class($this))`'s basename — would also have mismatched `plugin_settings.plugin_name` once any code instantiated the v2 class directly; this fix resolves that too, since the real instantiated object's class is now named exactly `ChatwootIntegrationPlugin` again.) **Fix**: renamed the old root `ChatwootIntegrationPlugin`/`ChatwootIntegrationPlugin.php` (v1 logic) to `ChatwootIntegrationBasePlugin`/`ChatwootIntegrationBasePlugin.php`; `ChatwootIntegrationV2Plugin` now extends it; a new, minimal root `ChatwootIntegrationPlugin.php` (`class ChatwootIntegrationPlugin extends ChatwootIntegrationV2Plugin {}`, deliberately never allowed to declare its own methods) now occupies the exact conventionally-guessed FQCN, so the real production instantiation path gets full v2 behavior. `index.php` updated to match for consistency on its own (now-dead-in-production, `deprecatedInstantiatePlugin()`-only) fallback path. New `tests/v2/plugin-instantiation-convention.php` proves the fix at the source level (the exact real naming-convention expression, the shell class's real content, and that it never grows its own methods). This is a genuine, RC-blocking finding for any real-world v1-to-v2 upgrade — closing it here is a direct, concrete product of the real OJS 3.5 runtime harness work, not something unit/source-string tests against a fabricated environment could ever have caught.

  **Second, related finding from the same live re-verification pass (after the fix above was merged and pulled onto the live demo):** the previously-404ing routes started responding, but as `302` redirects to `/login?source=...` rather than actually running — a real, different, still-wrong response. Root cause, confirmed against real pkp-lib source (`lib/pkp/controllers/page/PageHandler.php`): all three of this plugin's page handlers (`SupportGatewayPageHandler`, `SupportKnowledgePageHandler`, `McpGatewayPageHandler`) extended `PKP\controllers\page\PageHandler`, whose own `authorize()` override grants anonymous access only to its two built-in ops (`tasks`/`css`) — every other op, including every real op these handlers define, fell through to a login requirement, directly contradicting `SupportKnowledgePageHandler`'s own docblock ("must never require... any capability check to render") and `SupportGatewayPageHandler`'s/`McpGatewayPageHandler`'s own independent Bearer-token/CSRF auth design. **Fix**: all three now extend the plain app `Handler` instead, matching the real, already-proven-working pattern confirmed by reading `contributorUserSync`'s `ContributorApprovalHandler` on the same live box (extends `Handler` directly, declares no `authorize()` override). New `tests/v2/http-handler-public-access.php` proves this at the source level. **Final live re-verification on `ojs-demo.airixmedia.com` after both fixes, conclusive**: `GET /ajdsi/ojsMcpGateway` → real `200`, `content-type: application/json` (the actual MCP handler ran); `GET /ajdsi/support-knowledge/about` → real `200`, `content-type: text/html`, with a genuinely rendered page body ("AIRIX Journal of Digital Scholarship and Innovation — About", real nav links to the plugin's other real knowledge routes) — not a redirect, not a 404, the actual handler output; `GET /ajdsi` (regression check) stayed `200`; zero fatals/errors in the container log across the whole verification window. The entire TST-014 saga (routing bug + auth bug) is conclusively resolved end-to-end on a real, live OJS 3.5 install.
- [x] **TST-015** Package install/enable/disable/uninstall smoke test (first slice). — scoping decision, same character as TST-002/003: the real migration is Illuminate-based and the plugin's `getEnabled()` reads a real `plugin_settings` table through a live OJS runtime — neither is invokable here. New `tests/v2/tst-015-install-uninstall-enable.php` instead: (1) replicates all five real tables' DDL (verified in-test against the migration source, including the real reverse-dependency drop order in `down()`) against a real `pdo_sqlite` database, proving a real install → idempotent re-install → a real unique-constraint rejection (session `public_id`, beyond the event-queue table TST-002 already covered) → uninstall → idempotent re-uninstall lifecycle; (2) proves both real enable/disable gating call sites (`bindSupportSessionRequest()`, `supportGatewayUsable()`) share the same `!getEnabled($contextId) && !getEnabled()` short-circuit, and exercises `supportGatewayUsable()`'s remaining settings-completeness logic (pure boolean/string logic, fully testable without OJS) against every single-field-missing case.
- [x] **TST-016** Chatwoot Captain unavailable/disabled graceful-degradation test (first slice). — new `tests/v2/tst-016-captain-outage.php` applies the TST-003 real-socket pattern here: a minimal real-HTTP `ChatwootCaptainClientInterface` implementation (PHP's stream wrapper, no Guzzle/Composer needed — `ChatwootApiService` itself still can't be instantiated in this environment) is pointed at a real dead port for a genuine connection-refused, then driven through the real `CaptainDocumentProvisioner`/`CaptainProvisioningHealthService`. Proves: a real outage during provisioning never throws and resolves to the generic `failed` status with a safe fixed reason code (never raw exception/network text); the local `CaptainSyncState` record is left consistent (unresolved, never falsely "owned"); the health report (a pure local read, confirmed never itself making a network call) correctly classifies the outage as `failed`; an unrelated journal's health report is unaffected (no global lock/blocking state); and — via a real recovery server started after the outage — a retried `provision()` succeeds and correctly transitions local/health state from failed back to owned, proving retry bookkeeping is safe across a real outage-then-recovery cycle, not just during the outage. `CaptainDocumentProvisioner` has no delete path at all (confirmed by its own docblock and the `ChatwootCaptainClientInterface` contract, which defines no document-delete method), so "never mutates/deletes unrelated resources" holds structurally for this provisioner. Tool/scenario provisioners (`CaptainCustomToolProvisioner`/`CaptainScenarioProvisioner`) follow the identical create-or-detect-conflict/resync shape — a follow-up slice, not duplicated here.

## SEC — Security Hardening

- [x] **SEC-001** Threat-model review before Phase 2 merge. Re-reviewed in `docs/v2/SECURITY_PRIVACY.md` §3.1 against the surfaces built since the original list (Admin Console ADM-001–006, Event Bridge policy UI, trusted-proxy/rate-limit closure API-007): CSRF/authorization on new admin verbs verified structurally (they share v1's already-reviewed `manage()` gate), mail-test/dead-letter-retry/Captain-repair abuse potential checked and found bounded, Event Bridge opt-in enforcement confirmed fail-closed, trusted-proxy spoofing confirmed closed. No new unmitigated threat found.
- [x] **SEC-002** Forged Chatwoot header test. — `tests/v2/forged-chatwoot-header.php`: this plugin has no inbound Chatwoot webhook receiver and no code path anywhere in `classes/v2/` reads an `X-Chatwoot-*` header for identity/authorization (verified by grepping the entire real source tree, with a sanity check the search mechanism itself actually detects a known real header read). Conversation identity is instead re-derived by calling back into Chatwoot's own API with the server-side token and checking Chatwoot's own `meta.hmac_verified` response field — never a client-supplied header — so the vulnerability class this item targets structurally doesn't apply to this architecture.
- [x] **SEC-003** Cross-journal IDOR test. — `tests/v2/submission-verify.php` (submission in another journal resolves no relationship; earlier chatwoot-binding tests cover the conversation-binding side).
- [x] **SEC-004** Cross-conversation session replay test. — `tests/v2/support-session.php`: a session bound to one exact (context, account, contact, conversation) tuple is proven to reject resolution against a different conversation id, a different contact id, and a different journal — each independently, using the same bound session. Also covers the adjacent one-time binding-token replay (a consumed binding token must reject a second bind attempt) and cross-context/cross-user binding rejection.
- [x] **SEC-005** OTP brute-force/rate-limit test. — `tests/v2/external-verification.php`: brute-force lockout after `maxAttempts` (4) wrong PINs, with the genuinely correct PIN still rejected once locked (not a soft warning); per-conversation and per-identity rate limits independently exercised until each threshold is hit and `requestChallenge()` returns null.
- [x] **SEC-006** Prompt-injection/tool-abuse test. — `tests/v2/prompt-injection-tool-abuse.php`. Found and fixed a real gap: `HandoffSummaryFormatter::sanitizeReason()` stripped control characters but never newlines, so a crafted `reason` (the one field an attacker or a prompt-injected Captain relaying one can influence) could inject a fake `- Field: value` line or even a fake second `**Support Gateway Handoff**` header into the rendered private note, forging structured facts a human agent or a later-summarizing LLM might mistake for real. Fixed by collapsing newlines/tabs to spaces before capping — the reason's actual content is fully preserved, only its ability to spoof a new structural line is removed. Also asserts, source-wide, that every real `->allows(...)` authorization call site is always a fixed string capability name, never a variable derived from free text, and that the one endpoint carrying an attacker-influenceable field (submission title) evaluates its capability check before ever reading that field.
- [x] **SEC-007** Secrets absent from browser/logs/export. — covered across per-surface tests rather than one consolidated check: `tests/v2/live-plugin.php` (settings export redacts `chatwootApiAccessToken`/`chatwootIdentityValidationSecret`/`chatwootSupportApiToken`), `tests/v2/audit-logger.php` (the persisted audit sink allowlists exactly six safe fields, dropping anything else even on its DB-failure fallback path), and multiple serializer tests (`submission-support.php`/`publication-status.php`/`required-actions.php`/`blind-review-anonymity.php`) forbidding `email`/`reviewer_id`/`token`/etc. in their output.
- [x] **SEC-008** Public knowledge contains no protected data. — `tests/v2/knowledge-compiler.php`: a provider emitting a `KnowledgeClassification::PRIVATE` fact and an `UNSUPPORTED` fact are both proven to never survive into the compiled/rendered output — `KnowledgeCompiler` keeps only facts classified exactly `PUBLIC`.
- [x] **SEC-009** Dependency/security scan. Real local Semgrep scan (v1.175.0, installed in an isolated venv — no system-wide change) against `p/php`, `p/security-audit`, and `p/secrets` rulesets: 0 findings across 63–89 rules over the full tracked source tree (206 files, ~100% parsed, no rule-relevant files skipped — the "skipped" count was only files not tracked by git). No dependency manifest exists to scan for known-CVE packages (this plugin ships no `composer.json`/`package.json` of its own — it runs entirely inside a host OJS install's own dependency tree, which is that install's own responsibility, not this plugin's); this is a real environmental fact, not a skipped step.
- [x] **SEC-010** Security review before stable release. Re-run immediately before cutting the release, against the actual release commit (`7d39a20`): real local Semgrep scan (`p/php`, `p/security-audit`, `p/secrets`) — 0 findings across 63 rules, 214 tracked files, ~100% parsed; a targeted grep for secret-shaped literals across the tracked tree found none. Builds on the current SEC-001/SEC-009 baseline from this same session rather than starting cold.

## RELS — PKP Plugin Gallery Release

- [ ] **RELS-001** Repository is public before Gallery submission. — deliberately not actioned: this is a real publication action (this repo's own description names GitHub as "the publication target") gated by the standing PUBLIC RELEASE GATE — not something to toggle without explicit owner approval.
- [ ] **RELS-002** Public release package available for download. — same gate as RELS-001; a real publication action.
- [x] **RELS-003** Root GPL-compatible `LICENSE`. Already real and verified (FND-001): root `LICENSE`, GPL-3.0-or-later, SPDX-tagged, and CI's own "Release-source guards" step asserts this on every PR.
- [x] **RELS-004** Tests/CI green. Real and current: every merge into `v2-dev` this entire session (30+ PRs) required a green `v2 validation` CI run first — verified, not assumed.
- [x] **RELS-005** Build `.tar.gz` with a single `chatwootIntegration` directory. Final build via `git archive --format=tar --prefix=chatwootIntegration/ HEAD | gzip` from the actual release commit `5cc04bc` — real, verified single top-level directory, confirmed to extract cleanly (244 archive entries / 210 real files), confirmed no dev/CI content (see RELS-006's second finding below) and `version.xml`/`LICENSE`/`index.php` present. The built artifact lives outside version control — see `docs/v2/GALLERY_RELEASE_METADATA.md` for the exact final command/commit/checksums.
- [x] **RELS-006** Remove nonessential dependency-manager demos/examples/dev files. No `*.example`, demo, or `.env`-style file exists anywhere in this repo. **Two real gaps were found and fixed across this item's passes**: (1) two stale, informal v1-era working documents at the repo root, `STABILIZATION_SUMMARY.md`/`TEST_CHECKLIST.md`, unreferenced by any code/test/CI, superseded by `docs/v2/TEST_PLAN.md` and the real `tests/v2/*.php` suite — removed. (2) `git archive` was bundling `.forgejo/`, `.github/`, `.gitignore`, `.php-cs-fixer.php`, and the full 83-file `tests/v2/` dev suite directly into the real release package (295 archive entries before the fix) — none of it has any purpose inside an installed OJS plugin directory. Fixed with a new `.gitattributes` `export-ignore` (244 entries after), the correct, maintainable mechanism rather than a one-off packaging script.
- [x] **RELS-007** Four-part release version, e.g. `2.0.0.0`. `version.xml` already declares `2.0.0.0` (bumped in TST-014's real live-verified upgrade work).
- [x] **RELS-008** Exact OJS compatibility versions tested. Real, live-verified: OJS 3.5.0-5 + MySQL 8.0, a real install/upgrade cycle via `lib/pkp/tools/installPluginVersion.php` against `ojs-demo.airixmedia.com` (TST-004/RUN-001), plus real HTTP route verification after the TST-014 fixes. PHP 8.2/8.3 confirmed as the real supported range (TST-008).
- [ ] **RELS-009** Create immutable GitHub release/tag/package. — gated by the standing PUBLIC RELEASE GATE; a real, irreversible publication action.
- [x] **RELS-010** Calculate/record MD5 for Gallery XML. Final real MD5 (`752d100032f333ba9d142c485bbfd8ac`) and SHA-256 of the actual final RELS-005 tarball (from release commit `5cc04bc`), recorded in `docs/v2/GALLERY_RELEASE_METADATA.md`.
- [x] **RELS-011** Prepare title/summary/description/homepage/maintainer metadata. New `docs/v2/GALLERY_RELEASE_METADATA.md` — real title/summary/description drawn from the actual built v1+v2 feature set (cross-checked against the now-corrected `README.md`, not the stale v1-only version that predated this pass), the real current repo URL as homepage, real maintainer info matching this project's own commit-identity convention.
- [x] **RELS-012** Prepare Gallery XML release snippet. Same file — a draft `<release>` XML snippet with a real MD5, real version/date, and real OJS `3.5.0.0` compatibility (matching the actual tested target, never 3.6). The `<package>` URL is an explicit, clearly-labeled placeholder — no real package URL exists yet, and none is fabricated here.
- [ ] **RELS-013** Validate XML/package URL/MD5. — depends on RELS-002/009's real, gated publication existing first; the placeholder package URL cannot be validated yet.
- [ ] **RELS-014** Open PR to `pkp/plugin-gallery`. — gated by the standing PUBLIC RELEASE GATE; a real, public, external-repo action.
- [ ] **RELS-015** Address PKP automated checks/code review. — cannot happen before RELS-014 exists; a process step for after the gate.
- [ ] **RELS-016** Never modify an already-published Gallery release artifact. — a standing behavioral rule to honor once RELS-014 exists, not a deliverable to build now.
- [x] **RELS-017** Document compatibility-update process for future OJS releases. New section in `docs/v2/VERIFICATION_MATRIX.md` ("Compatibility-update process for a future OJS release") — the exact real process this release itself used (hook re-verification, PHP matrix re-verification, real install/upgrade re-proof, real Chatwoot API re-verification), so a future OJS-version pass has a concrete, evidenced checklist rather than starting cold.

## DOC — Documentation

- [x] **DOC-001** User install/config guide. — new `docs/v2/INSTALL_CONFIG_GUIDE.md`. Real requirements (cross-checked against `VERIFICATION_MATRIX.md`'s PHP matrix), the real install-directory-name constraint from TST-014's own finding, and a field-by-field Connection-section walkthrough with the exact real label/description text from `locale/en/locale.po`. Cross-references rather than duplicates `UPGRADE_FROM_V1.md`, `REST_API_GUIDE.md`, `MCP_SETUP_GUIDE.md`, `TROUBLESHOOTING_HEALTH_GUIDE.md`, and `VERIFICATION_SECURITY_ADMIN_GUIDE.md`.
- [x] **DOC-002** Core Chatwoot Bridge guide. — new `docs/v2/CORE_BRIDGE_GUIDE.md`, written directly from `ChatwootIntegrationBasePlugin::addChatwootWidget()`'s real source: the exact real custom-attribute payload sent on every page load, the real blind-review privacy-mode masking behavior (one-way hash/masked name/masked email/`is_masked` flag), and every real visibility/performance setting with its exact locale label and verified real effect (e.g. "Excluded Pages" matches the exact `getRequestedPage()` value, "Hide for Role" checks every role the user holds, not just a primary one).
- [x] **DOC-003** Captain Intelligence prerequisites/provisioning guide. — new `docs/v2/CAPTAIN_PREREQUISITES_GUIDE.md`, a short admin-facing summary (Chatwoot edition/plan, credential reuse, Support API token dependency, the Sync/Repair Captain action) that cross-references rather than duplicates `KNOWLEDGE_DIAGNOSTICS.md` §6's exhaustive technical provisioning detail.
- [x] **DOC-004** Verification/security admin guide. — new `docs/v2/VERIFICATION_SECURITY_ADMIN_GUIDE.md`. Honestly documents that verification parameters (PIN length, attempts, expiry) have no settings-form UI at all yet — they are fixed in code, not a currently-configurable admin control, despite `SECURITY_PRIVACY.md` §5 stating that as a future design principle — and covers the real, actually-configurable Event Bridge section (default delivery mode, the consent checkbox's real fail-closed behavior, per-event JSON overrides) with the exact real mode-value strings and event-type list, cross-checked against `EventDeliverySettingsResolver`'s real source before writing.
- [x] **DOC-005** Knowledge provider guide. — new `docs/v2/KNOWLEDGE_PROVIDER_GUIDE.md`, a short admin-facing summary of the real public knowledge categories (cross-checked against `KnowledgeRouteCatalog`'s real category-to-fact-prefix mapping, the single source both the page navigation and sitemap already generate from) and the one real classification rule that decides what's public. Cross-references rather than duplicates `KNOWLEDGE_DIAGNOSTICS.md`'s exhaustive technical model.
- [x] **DOC-006** MCP setup guide. — new `docs/v2/MCP_SETUP_GUIDE.md`, written now that the MCP surface has stabilized (MCP-001 through MCP-009 all done), per ADR-023's own stated sequencing ("user-facing docs follow once the externally visible MCP/OpenAPI/runtime surfaces have stabilized, not before"). Covers the endpoint/auth/protocol basics, the full built tool/resource tables, error codes, and honestly documents a real gap: `mcpServiceToken` has no settings-form field yet (unlike `chatwootBaseUrl`/`chatwootApiAccessToken`), so the guide gives the real generic OJS mechanism (`Plugin::updateSetting()`) rather than pointing at a UI field that doesn't exist.
- [x] **DOC-007** REST/OpenAPI guide. — new `docs/v2/REST_API_GUIDE.md`. Points at `openapi.json` as the authoritative machine-readable contract rather than duplicating its endpoint list (which would drift); covers auth (including real multi-token rotation support already in `ServiceTokenAuthenticator::verify()`), the conversation-tuple convention, the response envelope, and the anti-enumeration rule every resource-scoped endpoint shares.
- [x] **DOC-008** Troubleshooting/health guide. — new `docs/v2/TROUBLESHOOTING_HEALTH_GUIDE.md`, written directly from the real settings-form template/locale strings (ADM-001 through ADM-006) — every field label, button, and behavior described (Support Gateway Health section, Sync/Repair Captain, Retry Dead Letters, Send Test Email, secret masking, the older Health Check/Test Message/Export-Import buttons) matches the actual built UI, not a planned one; the secret-masking behavior described in §7 was re-verified against `SecretFieldMasking`'s real source before writing it.
- [x] **DOC-009** Upgrade from v1 guide. — new `docs/v2/UPGRADE_FROM_V1.md`, written directly from the real, live-verified upgrade procedure (TST-014/RUN-001): the exact `lib/pkp/tools/installPluginVersion.php` command, why the upgrade step is required (the real `PluginRegistry::instantiatePlugin()` convention-guessing behavior TST-014 uncovered), and the exact real verification queries (`versions` table, `SHOW TABLES LIKE 'chatwoot_support%'`) used live. Every step in it was actually run against a real OJS 3.5.0-5 + MySQL 8.0 install, not assumed.
- [x] **DOC-010** Privacy/data-retention guide. — new `docs/v2/PRIVACY_DATA_RETENTION_GUIDE.md`, written directly from the real migration (`InstallSupportGatewayMigration`) and the real `PurgeExpiredSupportDataTask` — a per-table what's-stored/what's-never-stored table, the exact real purge behavior (sessions/challenges on expiry, audit log at 90 days), and an honest, verified gap: `chatwoot_support_event_queue`/`chatwoot_support_knowledge_sync` rows are never purged by anything today. Also documents real uninstall behavior (`down()` drops all five tables).
- [x] **DOC-011** Gallery release runbook. New `docs/v2/GALLERY_RELEASE_RUNBOOK.md` — the real, exact repeatable steps already executed this pass (version bump, real `git archive` build, real MD5, drafted metadata) plus the real remaining publication steps (RELS-001/002/009/013/014/015/016), explicitly marked as owner-gated by the standing PUBLIC RELEASE GATE rather than something to execute autonomously.

## Deferred / explicit post-baseline ideas

These remain in the product vision but are not silently included in the initial v2 release:

- [ ] staff write actions such as review reminders/deadline changes;
- [ ] secure author file download links;
- [ ] duplicate-account staff resolution workflow;
- [ ] Crossref/DataCite-specific deep diagnostics;
- [ ] payment-provider-specific remediation actions;
- [ ] automatic support analytics/FAQ approval workflow;
- [ ] proactive WhatsApp/Telegram messaging beyond Chatwoot’s configured channel capabilities;
- [ ] any native Chatwoot MCP integration if upstream later implements one.